# Mined: video-notes, instagram-findings, misc research

Mined 2026-08-08. Sources read: video-notes/9-claude-code-plugins/{README.md,UNDERSTANDING.md}, video-notes/creator-starts-every-project/{README.md,UNDERSTANDING.md}, video-notes/karpathy-method/{README.md,UNDERSTANDING.md}, video-notes/loop-engineering/{README.md,UNDERSTANDING.md}, video-notes/most-popular-skills/{README.md,UNDERSTANDING.md}, video-notes/paste-this-into-claude/{README.md,UNDERSTANDING.md}, video-notes/vid8-tmp/{README.md,transcript.md}, research/instagram-findings.md, research/cutting-edge-ai.md, research/eliot-prince-credibility.md (read in full — a credibility assessment of a YouTube presenter; concludes no specific technique verdict, so contributes 0 rows), research/allocation-and-regime.md, research/alpha-discovery-gp-symbolic-regression.md, research/binance-withheld-streams.md, research/bull-bear-profit-agents-spec.md, research/continual-learning-and-failure-modes.md, research/crypto-alpha-decay-execution-costs.md, research/crypto-counterparty-operational-risk.md, research/crypto-exchange-fees-maker-economics.md, research/crypto-latency-cloud-vs-colo.md, research/crypto-native-market-structure.md, research/crypto-orderbook-signals.md, research/dual-agent-spec.md, research/finml-architectures-live-evidence.md, research/finml-feature-engineering.md, research/finml-gbt-vs-dl-by-frequency.md, research/finml-replication-problem.md, research/finml-subsecond-and-dontbuild.md, research/kronos-foundation-model.md, research/llm-code-evolution-funsearch-alphaevolve-adas.md, research/multiple-comparisons-false-discovery-backtesting.md, research/prior-attempts-postmortem.md, research/promotion-pipeline.md, research/risk-and-failure.md, research/search-infrastructure-pbt-nas-bayesopt-bandits.md, research/trading-feature-stores-experiment-tracking.md, research/trading-market-data-storage.md, research/trading-monitoring-deployment-discipline.md, research/trading-operational-failures.md, research/trading-reproducibility.md, research/trading-secrets-management.md, research/validation-beyond-deflated-sharpe.md, research/mcp-plugins-for-trading-stack.md (read in full; filtered to the one in-scope trading-architecture/security item — the rest of that file is Claude Code plugin-curation, out of scope).
Rows emitted: 520.

| Feature | What it does | Category | Evidence | State |
|---|---|---|---|---|
| Compound Engineering Loop | A plan → work → review → compound → repeat workflow where each unit of engineering work is meant to make the next one easier; roughly 80% of effort goes into planning/review, 20% into execution, and lessons are codified into reusable notes. | build-process | video-notes/9-claude-code-plugins/UNDERSTANDING.md — "The one genuinely valuable idea: Compound Engineering" | CAPTURED |
| Maintainer-Matters Heuristic | When choosing between tools/plugins, bias toward ones where someone is paid or otherwise incentivized to keep them working, since community-maintained tools can silently break. | build-process | video-notes/9-claude-code-plugins/UNDERSTANDING.md — "Two plugin-selection principles" | CAPTURED |
| Avoid Platform-Dependency Lock-in | Before building on top of a third-party tool, ask whether a platform-vendor release could kill it ("don't be one plugin from extinction") and reconsider the build if so. | build-process | video-notes/9-claude-code-plugins/UNDERSTANDING.md — "Two plugin-selection principles" | CAPTURED |
| Three-Layer Research Stack | Combine three distinct research mechanisms for different jobs: native keyword search (fetch a known URL), semantic search (find the right sources), and extraction (pull clean content, handling JS/nav/footer). | build-process | video-notes/9-claude-code-plugins/UNDERSTANDING.md — "The research stack (#2)" | CAPTURED |
| Model Independence / Task-Matched Model Selection | Deliberately practice using multiple different LLM providers/models rather than depending on one, since different models are good at different things and current pricing may be subsidized. | build-process | video-notes/9-claude-code-plugins/UNDERSTANDING.md — "Multi-model (#6)" | CAPTURED |
| Mechanical-Work-to-Script Conversion | Periodically audit which tasks are mechanical rather than reasoning-based and convert those into deterministic scripts instead of re-deriving them with an LLM each time. | build-process | video-notes/9-claude-code-plugins/UNDERSTANDING.md — "Mechanical vs. reasoning token spend (#8)" | CAPTURED |
| Plan Mode Before Execution | Start the large majority of work sessions in an explicit planning mode before any building/editing begins, so babysitting happens before the plan instead of throughout execution. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "1. Plan mode" | CAPTURED |
| Pre-Build Interview Technique | Before building anything, have the agent interview the user (core problem, who it's for/not for, success criteria, what it should NOT do, key decisions) and summarize the answers back before writing code. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "1. Plan mode"; also paste-this-into-claude/UNDERSTANDING.md — "3. Interview me"; karpathy-method/UNDERSTANDING.md — "Layer 1 — The Spec" | CAPTURED |
| Minimal CLAUDE.md / Delete-When-Bloated | Keep the standing instructions file as short as possible; when it grows bloated, delete it and start fresh, adding content back only when the model demonstrably goes off track without it. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "2. CLAUDE.md — minimal, and delete it when bloated" | CAPTURED |
| Post-Mistake CLAUDE.md Update Prompt | After the agent makes a mistake, prompt it to update the memory file so the same mistake doesn't happen again, and periodically prompt it to strip anything contradictory, duplicate, or bloated. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "2. CLAUDE.md" | CAPTURED |
| Verification Feedback Loop | Give the agent a tool that can see the output of its own work, and explicitly tell it that tool exists; this feedback loop is described as the single highest-leverage lever for result quality. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "3. Verification"; also karpathy-method/UNDERSTANDING.md — "Layer 2 — The Verifier"; paste-this-into-claude/UNDERSTANDING.md — "4. Verify before you build" | CAPTURED |
| Mid-Session Re-Verification Prompt | Mid-task, prompt the agent to go back and verify all of its work so far, checking that it used best practices, was efficient, and introduced no issues. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "3. Verification" | CAPTURED |
| Parallel Partitioned Sessions | Run multiple agent sessions at once, each on a non-overlapping task, since two agents touching the same files clash and waste effort. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "4. Multiply yourself — parallel, partitioned sessions" | CAPTURED |
| Fresh-Session Reset for Stuck Problems | When a session gets stuck, start a brand-new session on the same problem rather than continuing, since the original context can be "fogging" the model's judgment. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "4. Multiply yourself" | CAPTURED |
| Inner-Loop-to-Slash-Command Conversion | Convert any workflow performed many times a day ("inner loop") into a slash command so it doesn't require repeated prompting. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "5. Inner loops → slash commands / skills" | CAPTURED |
| Skill as Reusable Procedure | Package a validated, repeated workflow as a skill — a fixed "play to run" that executes the same way every time, as opposed to a prompt that merely asks the model to think. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "5. Inner loops → slash commands / skills"; also most-popular-skills/UNDERSTANDING.md — "The one genuinely useful idea" | CAPTURED |
| Skill-Candidate Discovery Prompt | Ask the agent, based on the project being worked on, what skills should be created — a generic discovery prompt for identifying skill candidates. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "5. Inner loops → slash commands / skills" | CAPTURED |
| Never-Bet-Against-the-Model / Information Moat Investment | Treat scaffolding, micro-tweaks, and heavily optimized prompts as likely to be unnecessary as models improve, and instead invest effort in the durable data/system feeding the model. | build-process | video-notes/creator-starts-every-project/UNDERSTANDING.md — "6. Build for the future — 'never bet against the model'" | CAPTURED |
| Three-Layer Build Framework | Structure agent-building work around three durable layers instead of one-off prompts: the Spec (what/why), the Verifier (how do we know it's right), and the Environment (where it lives so it compounds). | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Core thesis" | CAPTURED |
| Animal-vs-Ghost Framing | Treat the AI as a non-intentional system (a "robot librarian" that answers only from what it has and doesn't know when something is missing) rather than as a colleague that responds to pressure or encouragement. | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "The framing problem: 'animals vs ghosts'" | CAPTURED |
| Uncover the Goal, Not the Task | Distinguish the stated task from the underlying goal/decision it is meant to support, since the agent can never decide the goal itself — only the human can. | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Layer 1 — The Spec, 1. Uncover the goal" | CAPTURED |
| Agile Spec-Building | Bias toward smaller, compartmentalized specs with visible checkpoints ("agile") rather than handing over the whole job and only seeing the result at the end ("waterfall"). | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Layer 1 — The Spec, 2. Be agile, not waterfall" | CAPTURED |
| Precise, Explicitly-Verified Spec Decisions | Write specs precisely and explicitly ask the agent to verify key decisions with the human, since every unstated assumption is a chance for the build to drift. | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Layer 1 — The Spec, 3. Be precise, and use your own brain" | CAPTURED |
| Define Evaluation Criteria Up Front | Before the agent touches anything, define precise evaluation criteria for what a high-quality final product looks like, rather than vague criteria like "make it look good." | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Layer 2 — The Verifier, 1. Define evaluation criteria UP FRONT" | CAPTURED |
| Second-Model-as-Critic Verification | Route completed work through a second, different model (or a separate subagent/judge) to check it, since a different "library" may catch what the first model cannot. | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Layer 2 — The Verifier, 2. Use a second model as critic"; also loop-engineering/UNDERSTANDING.md — "Block 3, Pro tip" | CAPTURED |
| External-Signal Verification | Don't let the agent merely assert that a result (e.g. a deploy) succeeded — connect it to the real external target/system so it can check against reality instead of imagination. | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Layer 2 — The Verifier, 3. Pull external signal" | CAPTURED |
| Structured CLAUDE.md Content | Organize the standing instructions file into distinct sections: how the repo works, a skill-routing table (which trigger maps to which skill), knowledge architecture, and hard working rules. | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Layer 3 — The Environment, 1. A real CLAUDE.md" | CAPTURED |
| Machine-Enforced Pipeline Status Gate | Use a status field (e.g. YAML frontmatter) that a downstream automation checks and refuses to proceed until an upstream step has set the required status, making a pipeline order machine-enforced rather than advisory. | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Layer 3 — The Environment, 1. A real CLAUDE.md" | CAPTURED |
| LLM Knowledge Base as Data Moat | Build a folder system holding your own material, organized so the agent can find things — treating your own curated data as the durable competitive asset. | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Layer 3 — The Environment, 2. Build an LLM knowledge base" | CAPTURED |
| Guide vs. Rule Enforcement | Distinguish a "guide" (a CLAUDE.md instruction the agent can still ignore) from a "rule" (the same instruction enforced at the tool level, e.g. a PreToolUse hook that blocks a Write/Edit outright, so it cannot be crossed). | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Layer 3 — The Environment, 4. Guardrails — the guide/rule distinction" | CAPTURED |
| Three-Bucket Action Classification | Classify every possible agent action into "Always do" (autopilot, allowlisted), "Ask first" (needs human confirmation), or "Never do" (blocked by a hook, not prose). | build-process | video-notes/karpathy-method/UNDERSTANDING.md — "Layer 3 — The Environment, 4. Guardrails" | CAPTURED |
| 4-Condition Loop-Readiness Test | Before building any autonomous loop, require yes on all four: does the task repeat, is there a clear definition of done, can you afford wasted/repeated runs, and does the agent have the tools/data it needs. | build-process | video-notes/loop-engineering/UNDERSTANDING.md — "Part 1 — The 4-Condition Test" | CAPTURED |
| Loop Trigger Mechanism Selection | Choose how a loop is triggered based on tradeoffs: a local trigger (simple, dies when the machine is off), a cloud/scheduled trigger (runs remotely on a cadence), or a custom orchestration skill (invoked on demand, holding the goal/steps/done-rule/verification together). | build-process | video-notes/loop-engineering/UNDERSTANDING.md — "Part 2 — Four building blocks, Block 1: The trigger" | CAPTURED |
| Skill-Driven Loop Development | Never build an autonomous loop without battle-tested skills behind each step; the skills carry validated, specific judgment/preferences, and the loop only composes them. | build-process | video-notes/loop-engineering/UNDERSTANDING.md — "Block 2: Execution skills — 'skill-driven loop development'" | CAPTURED |
| Bridge Abstract Goals to Verifiable Output | Convert a fuzzy, unmeasurable goal ("is this good?") into something checkable by making the last step of the relevant skill emit a binary approved/not-approved result or a numeric score. | build-process | video-notes/loop-engineering/UNDERSTANDING.md — "Block 3: Goal + verification (inseparable)" | CAPTURED |
| Goal-Verification Inseparability | Treat a goal as invalid unless there is also a defined way to verify that the goal was completed — the two must be specified together, never a goal alone. | build-process | video-notes/loop-engineering/UNDERSTANDING.md — "Block 3: Goal + verification (inseparable)" | CAPTURED |
| Loop Output+Memory Logging | Record what happened on each loop run (a markdown file of lessons learned and run history) so subsequent runs don't repeat the same mistakes or re-derive the same context from scratch. | build-process | video-notes/loop-engineering/UNDERSTANDING.md — "Block 4: Output + memory" | CAPTURED |
| Build Loops from Smallest Proven Workflow | When starting to build a loop, begin from the smallest task you've already done manually and proven works, rather than an untested end-to-end process. | build-process | video-notes/loop-engineering/UNDERSTANDING.md — "Part 3 — Building your first loop" | CAPTURED |
| Loop Training Mode | A staged-autonomy pattern for a new loop/orchestration skill: while "ON," pause at every step for human approval, skip steps that already pass the done-rule, only re-run failing steps, and cap retries so it cannot loop forever; while "OFF," run autonomously but keep the done-rule checks and retry cap. Default is ON. | build-process | video-notes/loop-engineering/UNDERSTANDING.md — "Loop Training Mode — the guardrail" | CAPTURED |
| Checkpoints for Fuzzy/Unquantifiable Goals | When a goal is less quantifiable, break the loop into smaller sub-goals with human checkpoints placed at the decisions that shape everything downstream (e.g. date/venue/theme before planning a whole event). | build-process | video-notes/loop-engineering/UNDERSTANDING.md — "Checkpoints for fuzzy goals" | CAPTURED |
| Cap Installed Skills/Plugins to Limit Context Cost | Deliberately limit the number of installed skills/plugins (cited target: roughly three to five matched to actual workflow) since every added skill consumes standing context window on every turn. | build-process | video-notes/most-popular-skills/UNDERSTANDING.md — "The one genuinely useful idea" | CAPTURED |
| Third-Party Plugin/Skill Security Audit | Treat installed plugins/skills as highly trusted components that run code with your permissions, and audit/fingerprint third-party hook code rather than trusting it by default, since malicious skills have been documented hijacking tool routing. | build-process | video-notes/most-popular-skills/UNDERSTANDING.md — "The one genuinely useful idea" (closing security quote) | CAPTURED |
| Explicit Subagent Launch Instruction | Explicitly instruct the agent to launch subagents for a task (e.g. "launch five sub-agents to handle this"), since the main agent under-uses subagents by default and tends to do everything itself. | build-process | video-notes/paste-this-into-claude/UNDERSTANDING.md — "1. 'Launch sub agents'" | CAPTURED |
| Subagents for Diverse Independent Perspectives | Use multiple separate subagents (rather than repeated prompts in one chat) to get genuinely diverse perspectives on the same input, because asking the same model repeatedly in one context causes it to anchor on its own prior answers. | build-process | video-notes/paste-this-into-claude/UNDERSTANDING.md — "1. 'Launch sub agents'" | CAPTURED |
| Implementation Spec Before Build | Before any building starts, have the agent write a spec answering what it does, who it's for/not for, success criteria, out-of-scope items, and the key decision at each build step — collapsing a large combinatorial option space down to one concrete plan. | build-process | video-notes/paste-this-into-claude/UNDERSTANDING.md — "2. 'Write me an implementation spec'" | CAPTURED |
| CLAUDE.md Verification Rule + Blast-Radius / Human Validation Zones | Add a standing rule requiring the agent to state how it would verify work before starting and report the verification after finishing, plus a rule that before touching a designated high-cost-of-error zone it must ask first and explain the "blast radius" of the change. | build-process | video-notes/paste-this-into-claude/UNDERSTANDING.md — "4. 'Verify before you build'" | CAPTURED |
| Build Skills Only From Just-Validated Work | Build a skill from a conversation/workflow you just completed manually (already validated use case) rather than asking abstractly what skills should exist, which tends to produce skills that never get used. | build-process | video-notes/paste-this-into-claude/UNDERSTANDING.md — "5. 'Based on this conversation, build me a skill'" | CAPTURED |
| Gotchas Section in Skills | Maintain a "gotchas" section inside each skill capturing edge cases, stylistic quirks, and corrections encountered, appended to over time so the same mistake isn't repeated. | build-process | video-notes/paste-this-into-claude/UNDERSTANDING.md — "5. 'Based on this conversation, build me a skill'" | CAPTURED |
| Taste Test for Automation Candidacy | Before automating a task, ask whether judging good-vs-bad output requires human taste (favor augmentation instead) or is entirely quantifiable (candidate for automation). | build-process | video-notes/paste-this-into-claude/UNDERSTANDING.md — "6. 'Automate this' — the cautionary one, Filter 1" | CAPTURED |
| 80/20 Output-Quality Automation Filter | Before automating a task, ask whether an output that is only 80% as good as your own would still be acceptable; if yes, automate, if no, augment instead. | build-process | video-notes/paste-this-into-claude/UNDERSTANDING.md — "6. 'Automate this', Filter 2" | CAPTURED |
| Prefer Augmentation Over Full Automation | Default to augmenting (streamlining) a process rather than fully automating (removing the human from) it, because each automation adds ongoing operational debt to manage. | build-process | video-notes/paste-this-into-claude/UNDERSTANDING.md — "6. 'Automate this' — the cautionary one" | CAPTURED |
| Model Tiering by Task Complexity/Stakes | Match model choice (Haiku / Sonnet / Opus / Fable) to the task rather than defaulting to the most powerful model: Haiku for simple retrieval/summarization, Sonnet for everyday context-aware execution work, Opus for deep reasoning/high-stakes analysis with fewer mistakes, reserving the top tier to conserve usage budget. | build-process | video-notes/vid8-tmp/transcript.md — [01:33]-[07:43] segment (model breakdown) | CAPTURED |
| Effort-Level and Extended-Thinking Tuning | Choose an effort level (low/high/max) and toggle extended thinking per task rather than always maxing both, since higher effort/thinking increases response time and usage burn without proportional benefit on simple tasks; default to "high" when unsure. | build-process | video-notes/vid8-tmp/transcript.md — [15:01]-[16:33] segment | CAPTURED |
| Autonomous Top-Tier Model for Open-Ended Goals | Reserve the highest-tier, most autonomous model for hard, minimally-specified problems: give it a goal rather than detailed steps, and let it plan, pull from connected data sources, execute, hit dead ends, replan, and continue end-to-end with little re-prompting. | build-process | video-notes/vid8-tmp/transcript.md — [05:09]-[08:47] segment | CAPTURED |
| Three-bot architecture (BULL/BEAR/PROFIT-TAIL) | Splits directional-long, directional-short, and entry-timing/exit-management into three fully independent bots (own features, model, training pipeline, validation history) rather than heads on a shared trunk. | strategy | bull-bear-profit-agents-spec.md §0/§1 | CAPTURED |
| BULL agent | Proposes long-only trades, emits calibrated P(up) plus a conviction score; cannot propose a short. | strategy | bull-bear-profit-agents-spec.md §1 | CAPTURED |
| BEAR agent | Proposes short-only trades, emits calibrated P(down) plus a conviction score; cannot propose a long. | strategy | bull-bear-profit-agents-spec.md §1 | CAPTURED |
| PROFIT-TAIL agent | Owns entry timing and the entire position after fill (advisory expectancy/tail estimates feed the arbiter); explicitly cannot reject a selected trade or refuse to close a loser. | execution | bull-bear-profit-agents-spec.md §1/§2 | CAPTURED |
| Trade-selection authority belongs to the arbiter only | PROFIT-TAIL's expectancy/tail numbers are inputs, never a veto — splitting or exempting risk authority is named as the root cause of every blow-up in the research set. | governance | bull-bear-profit-agents-spec.md §2 rule 1 | CAPTURED |
| Hard stop overrides everything | The hard stop sits outside the whole authority chain and always wins, regardless of what any bot wants. | risk | bull-bear-profit-agents-spec.md §2 rule 3, diagram | CAPTURED |
| Every order re-enters the risk gate | No bot has a direct line to execution; entries, adds, scale-outs and closes all pass through the shared risk gate. | risk | bull-bear-profit-agents-spec.md §2 rule 4 | CAPTURED |
| Reduce-only exits | Closes are always reduce-only so a close can never accidentally open the reverse position. | execution | bull-bear-profit-agents-spec.md §2 rule 4 | CAPTURED |
| Request-add, never take-add | PROFIT-TAIL may signal "consider adding" but the arbiter and risk gate decide like any fresh entry; it may never unilaterally flip or add to a position. | governance | bull-bear-profit-agents-spec.md §2 rule 5 | CAPTURED |
| Shared raw market-data lake | Single ingestion path shared by all bots so they cannot silently disagree about what the price was. | market-data | bull-bear-profit-agents-spec.md §3 | CAPTURED |
| Separate feature namespace per bot | Feature computation is where each bot's independence actually lives, kept in a separate namespace per bot. | feature-engineering | bull-bear-profit-agents-spec.md §3 | CAPTURED |
| Separate model architecture per bot | Each bot is free to use a different winning architecture from its own bake-off. | models | bull-bear-profit-agents-spec.md §3 | CAPTURED |
| Separate training pipeline & retrain cadence per bot | A signal bot and an exit bot do not retrain on the same clock. | models | bull-bear-profit-agents-spec.md §3 | CAPTURED |
| Separate model registry entries per bot | Enables independent promotion and independent rollback per bot. | governance | bull-bear-profit-agents-spec.md §3 | CAPTURED |
| Shared joint Trial Registry across bots | All three bots' trials are counted together so the deflated Sharpe correction uses the honest joint trial count; separating registries would quietly restore overfitting. | governance | bull-bear-profit-agents-spec.md §3, Trap 3 | CAPTURED |
| Shared risk gate / execution / ledger, no exemptions | These three subsystems are never per-bot and carry no exemptions for any bot. | risk | bull-bear-profit-agents-spec.md §3 | CAPTURED |
| Mandatory model bake-off vs GBT and linear baseline | Every bot's deep architecture must beat gradient-boosted trees and a mandatory linear baseline out-of-sample on this system's own data before it gets capital. | models | bull-bear-profit-agents-spec.md §3 | CAPTURED |
| Per-bot competency level, independently gated | Each bot carries its own promotion/competency level; a bot at competency 2 does not get capital because a sibling is at 5. | governance | bull-bear-profit-agents-spec.md §3 | CAPTURED |
| `time_the_entry` function | Decides enter-now vs keep-waiting plus a limit price; authoritative but bounded by signal expiry. | execution | bull-bear-profit-agents-spec.md §4 Functions | CAPTURED |
| `ratchet_profit_lock` function | Returns a new lock level that is monotone and moves only in the favourable direction; authoritative. | risk | bull-bear-profit-agents-spec.md §4 Functions | CAPTURED |
| `decide_position_action` function | Returns HOLD / SCALE_OUT / CLOSE / REQUEST_ADD; authoritative, post-fill only. | execution | bull-bear-profit-agents-spec.md §4 Functions | CAPTURED |
| `estimate_net_expectancy` function | Advisory estimate of expectancy net of fees, funding, slippage and impact, feeding the arbiter. | strategy | bull-bear-profit-agents-spec.md §4 Functions | CAPTURED |
| `estimate_loss_tail` function | Advisory estimate of extreme-loss exposure (cascade, gap, liquidation distance) feeding the arbiter and risk gate. | risk | bull-bear-profit-agents-spec.md §4 Functions | CAPTURED |
| `forecast_profit_tail` function | Internal forecast of the full forward P&L distribution (not a point estimate) driving the three authoritative calls. | models | bull-bear-profit-agents-spec.md §4 Functions | CAPTURED |
| Joint entry-timing policy (price level + flow confirmation) | PROFIT-TAIL times entry on two signals learned jointly — a favourable price level and order-flow confirmation that a move is actually starting. | execution | bull-bear-profit-agents-spec.md §4 Entry timing | CAPTURED |
| Signal time-in-force / expiry bound on waiting | Every signal carries a deadline; if it expires unfilled the trade is abandoned and logged, so "not yet" cannot become a silent, unbounded rejection. | risk | bull-bear-profit-agents-spec.md §4 Entry timing | CAPTURED |
| Missed-entry rate as monitored metric | First-class monitored metric flagging a bot that misses the biggest movers even while its fill prices look excellent. | observability | bull-bear-profit-agents-spec.md §4 Entry timing | CAPTURED |
| Monotone profit-lock ratchet | The profit lock moves only in the favourable direction and never widens under any condition, for any model output. | risk | bull-bear-profit-agents-spec.md §4 The profit lock | CAPTURED |
| Volatility-scaled, learned lock distance | Lock distance is a function of realised volatility and the position's own path rather than a fixed percentage, which would either strangle the tail or fail to protect depending on regime. | risk | bull-bear-profit-agents-spec.md §4 The profit lock | CAPTURED |
| Venue-side mirrored profit lock | Lock rests at the exchange as a reduce-only stop order, continuously reconciled against local state, so it survives a bot-process death. | risk | bull-bear-profit-agents-spec.md §4 The profit lock | CAPTURED |
| Risk-gate-owned hard stop below the lock | A hard stop sits beneath the profit lock, owned by the risk gate, always active from fill and never moved by PROFIT-TAIL. | risk | bull-bear-profit-agents-spec.md §4 The profit lock | CAPTURED |
| PROFIT-TAIL-specific data requirements | Needs realised post-entry path data, live microstructure at decision time, its own fill history, funding accruals at real settlement times, and counterfactuals of its own past decisions. | feature-engineering | bull-bear-profit-agents-spec.md §4 Data it needs | CAPTURED |
| PROFIT-TAIL architecture candidates | Survival/hazard model over holding time, quantile/distributional regression on forward P&L, optimal-stopping formulation, and deep sequence models over the position's own path — all baked off. | models | bull-bear-profit-agents-spec.md §4 Architecture candidates | CAPTURED |
| Mandatory deterministic baseline for PROFIT-TAIL | Must beat immediate entry, fixed triple-barrier, and ATR trailing stop out-of-sample to get capital. | validation | bull-bear-profit-agents-spec.md §4 Architecture candidates | CAPTURED |
| Direction-filtered training data destroys calibration (lesson) | Training BULL only on up-moves means it never sees the negative class and cannot emit a calibrated probability; BULL/BEAR must train on all data with specialisation living in objective/action-space, not a filtered dataset. | models | bull-bear-profit-agents-spec.md §5 Trap 1 | CAPTURED |
| Win-rate objective inverts PROFIT-TAIL's design (lesson) | Training on hit rate teaches the bot to cut winners early and hold losers — the opposite of harvesting a profit tail; objective must be expectancy/tail capture, with the hard stop as a fixed constraint, never win rate. | models | bull-bear-profit-agents-spec.md §5 Trap 2 | CAPTURED |
| Three-way P&L attribution (signal / timing / exit alpha) | Because a trade's P&L mixes direction, timing and exit, BULL/BEAR are trained on fixed triple-barrier counterfactual labels rather than realised outcomes, and P&L attribution splits into signal alpha, timing alpha, and exit alpha as separate lines. | observability | bull-bear-profit-agents-spec.md §5 Trap 4 | CAPTURED |
| "Not yet forever" is a silent rejection (lesson) | Restates that unbounded waiting by PROFIT-TAIL would smuggle in the veto authority it was explicitly denied; solved by signal expiry plus the missed-entry metric. | governance | bull-bear-profit-agents-spec.md §5 Trap 5 | CAPTURED |
| Twin-bot correlation is not confidence (lesson) | BULL and BEAR trained over the same raw data anti-correlate trivially, so apparent agreement looks like confidence when it is really shared input; the arbiter must train on the joint distribution. | models | bull-bear-profit-agents-spec.md §5 Trap 7 | CAPTURED |
| Three independent pipelines triple compute/ops cost (stated cost) | Running three independent training/validation pipelines per brain costs roughly 3x the compute, storage and operational surface of one — accepted only if each bot clears its own promotion gate on its own evidence. | build-process | bull-bear-profit-agents-spec.md §5 (stated cost) | CAPTURED |
| Multi-brain tree (hours→minutes / minutes→seconds / sub-second) | Three brains, each running its own BULL/BEAR/PROFIT-TAIL triad, because a 4-hour entry/exit policy is nothing like a 4-second one — one shared model over all horizons learns the horizon with the most data and fails the others. | strategy | bull-bear-profit-agents-spec.md §6 | CAPTURED |
| Portfolio netting layer across brains | Nets exposure across brains and enforces total limits so two brains taking opposite sides of the same symbol don't pay fees both ways. | portfolio | bull-bear-profit-agents-spec.md §6 | CAPTURED |
| Phased rollout with deterministic-policy fallback | P1 ships BULL/BEAR with a deterministic exit policy (triple barrier + ATR trail + monotone ratchet); PROFIT-TAIL only replaces it after proving out in shadow and clearing the promotion gate per function/position class; the deterministic policy remains the permanent rollback fallback. | build-process | bull-bear-profit-agents-spec.md §7 | CAPTURED |
| Shadow-mode validation of PROFIT-TAIL before live | PROFIT-TAIL runs in shadow against the deterministic policy, with entry timing and exit policy shadowed and promoted separately. | validation | bull-bear-profit-agents-spec.md §7 | CAPTURED |
| Representation learning ("pizza / not pizza") | Model discovers its own predictive structure from raw/derived market data instead of being handed hand-written indicators. | models | dual-agent-spec.md §1 | CAPTURED |
| Gramian Angular Field (GAF) encoding | Encodes temporal correlation of a time series as a 2D image for CNN classification. | feature-engineering | dual-agent-spec.md §1 | CAPTURED |
| Markov Transition Field (MTF) encoding | Encodes state-transition probabilities of a time series as a 2D image for CNN classification. | feature-engineering | dual-agent-spec.md §1 | CAPTURED |
| Recurrence Plot encoding | Encodes when a dynamical system revisits prior states as a 2D image for CNN classification. | feature-engineering | dual-agent-spec.md §1 | CAPTURED |
| Rendered candlestick charts fed to CNN | Feeds rendered candlestick images directly into a vision architecture (ResNet/EfficientNet-style) to classify up/down/no-move. | feature-engineering | dual-agent-spec.md §1 | CAPTURED |
| Image-CNN path gated behind baseline (caveat) | Consistent with the ML research that GBT on tabular features usually beats deep learning on financial data, the image-CNN approach is built and tested — not assumed — and must beat LightGBM and linear baselines before capital. | validation | dual-agent-spec.md §1 | CAPTURED |
| Three-state output space LONG/SHORT/FLAT | Correct action space is three states, not two; flat is frequently the correct, profitable answer — avoids the "always in a trade" fee-bleeding design flaw. | strategy | dual-agent-spec.md §2 | CAPTURED |
| Arbiter via meta-labeling | A secondary model decides whether to act on the primary directional signal and at what size (López de Prado meta-labeling), decoupling "is there a signal" from "is it worth trading net of costs"; cited as one of the few production-proven techniques in the research set. | governance | dual-agent-spec.md §2 The arbiter | CAPTURED |
| Arbiter contradiction rule | When BULL and BEAR both fire high simultaneously, the arbiter forces FLAT rather than trading a self-contradiction; both low is also FLAT (no edge). | strategy | dual-agent-spec.md §2 Arbiter logic table | CAPTURED |
| Mandatory calibration before sizing | Raw softmax outputs are systematically overconfident (Guo et al. 2017) and overbet a Kelly-style sizing formula; temperature/Platt/isotonic calibration is required before any confidence-scaled sizing. | risk | dual-agent-spec.md §2 / risk-and-failure.md §4 | CAPTURED |
| Options are not a directional bet (lesson) | A call is not "long" and a put is not "short" — you can be exactly right on direction and still lose the whole premium to theta decay or IV collapse; named as the single most likely way this design loses money while the direction call is correct. | strategy | dual-agent-spec.md §3 | CAPTURED |
| Implied-volatility surface requirement | Options agents require IV by strike and expiry, not a single number. | feature-engineering | dual-agent-spec.md §3 | CAPTURED |
| Greeks computed and risk-managed | Delta, gamma, vega, theta tracked and risk-managed at both position and portfolio level, with aggregate vega sitting under the risk gate. | risk | dual-agent-spec.md §3 | CAPTURED |
| Volatility forecast independent of direction forecast | Options trade is only good if the option is cheap relative to expected realised vol, requiring a separate vol forecast compared against IV. | models | dual-agent-spec.md §3 | CAPTURED |
| Strike/expiry selection as explicit decision | A correct direction call with the wrong strike or expiry still loses, so strike/expiry choice is modeled as its own decision. | strategy | dual-agent-spec.md §3 | CAPTURED |
| Crypto options liquidity awareness | Crypto options are thin outside BTC/ETH near-the-money; Deribit is ~85-90% of BTC/ETH options volume and everything else has wide spreads. | market-data | dual-agent-spec.md §3 | CAPTURED |
| Margin/liquidation asymmetry table | Documents max-loss differences across spot, spot margin, futures/perps, and long/short options (from 100%-of-position up to unbounded). | risk | dual-agent-spec.md §4 | CAPTURED |
| Stricter short-side limits and liquidation-distance monitoring | BEAR's short futures exposure is unbounded on the upside (short squeeze has no ceiling), so position limits and liquidation-distance monitoring must be stricter on the short side than the long side. | risk | dual-agent-spec.md §4 | CAPTURED |
| Short options excluded from default action space | Selling calls/puts carries unbounded risk and is excluded by default, requiring an explicit, separately-gated promotion rather than being treated as "the other side" of buying options. | governance | dual-agent-spec.md §4 | CAPTURED |
| Perp funding as a real, unsmoothed P&L line | A long perp held through positive funding pays continuously; funding must be in the cost model, charged at actual venue settlement times (1h/4h/8h), not smoothed. | execution | dual-agent-spec.md §4 | CAPTURED |
| No exemptions from the pre-trade risk gate (lesson) | Every path — every brain, every direction, every instrument — passes through the same pre-trade risk gate with no exemptions, citing FTX/Alameda and bZx's "overcollateralized" carve-out as the two documented failures of exactly this. | governance | dual-agent-spec.md §5 | CAPTURED |
| Deflated Sharpe Ratio (DSR) gate | Corrects observed Sharpe for the inflation caused by testing N variants and keeping the best, deflating by the expected maximum Sharpe under the null given trial count and variance. | validation | promotion-pipeline.md "Why the gate exists" | CAPTURED |
| Immutable experiment ledger logging failures | Every experiment, including failures, must be logged immutably so N stays honest for DSR; silent discard of losers reintroduces the bias the gate exists to catch. | governance | promotion-pipeline.md "Why the gate exists" | CAPTURED |
| Stage 0 — unrestricted experimentation | Any strategy family, model class, timeframe, feature set or hyperparameter can be tried with no approval needed; every trial is written to the ledger (id, hypothesis, config, code hash, data window, seed) before it runs, trained on the training window only. | build-process | promotion-pipeline.md "Stage 0" | CAPTURED |
| Stage 1 — purged cross-validation | Uses purged K-fold with embargo or CPCV rather than standard k-fold, because financial data is serially correlated and overlapping-window labels leak across folds; kills on negative mean Sharpe or indistinguishability from the linear/LightGBM baseline. | validation | promotion-pipeline.md "Stage 1" | CAPTURED |
| Stage 2 — held-out test touched once | A chronological, embargoed block never used in Stage 0/1 and touched exactly once; requiring a second look invalidates the strategy since going back makes it training data. Realistic costs (fees, slippage, partial fills, funding, latency) are mandatory here. | validation | promotion-pipeline.md "Stage 2" | CAPTURED |
| Stage 3 — DSR + Probability of Backtest Overfitting | Computes DSR with the honest ledger-derived N plus PBO via CPCV; kills on non-significant DSR or PBO above threshold — the stage that catches "we tested 10,000 things and this one looked good." | validation | promotion-pipeline.md "Stage 3" | CAPTURED |
| Stage 4 — forward paper trading, model frozen | Live market data with simulated fills and a frozen model — the one test provably free of leakage; minimum duration set by trade-count-based statistics (PSR/MinTRL) and CPCV regime coverage, not calendar days or a fixed "N months" figure (none exists). Kills on live performance outside the statistical bounds implied by backtest (expect ~50% decay as normal; worse than that is broken). | validation | promotion-pipeline.md "Stage 4" | CAPTURED |
| Drawdown as a detector, not just a loss | Zero-Sharpe expected drawdown grows unboundedly as √T while positive-Sharpe drawdown grows only logarithmically in T, so drawdown depth relative to that envelope — not elapsed time alone — signals whether a strategy is broken. | observability | promotion-pipeline.md "Stage 4" / risk-and-failure.md addendum "drawdown formula" | CAPTURED |
| Stage 5 — gated live (small) | Requires the user's manual go-live control ("the button"); hard caps on capital fraction, per-strategy position limit, daily loss limit and kill switch; runs alongside its paper twin, with paper/live divergence itself monitored as a signal of real slippage vs modelled. | governance | promotion-pipeline.md "Stage 5" | CAPTURED |
| Stage 6 — scaled live | Position size increases only on sustained live performance tracking expectation, under continuous monitoring. | governance | promotion-pipeline.md "Stage 6" | CAPTURED |
| Promotion as a lease, not a deed (decay handling) | A strategy that worked does not stay working as edges get crowded out; rolling Sharpe is monitored against its promotion-time baseline, feature/prediction drift is monitored, and a statistical decay test distinguishes normal losing streaks from a broken model. | governance | promotion-pipeline.md "Decay" | CAPTURED |
| Demote-on-decay, never delete | On detected decay a strategy is demoted to paper and re-enters the pipeline rather than being deleted; its record stays in the ledger and still counts toward N. | governance | promotion-pipeline.md "Decay" | CAPTURED |
| Bounded definition of "self-improving" | System may generate hypotheses, test them, run the full gate, retire decayed strategies and reallocate capital toward what measurably works — but must never edit live model weights from its own P&L without passing the gate; that pattern is flagged as having no credible production example and as a self-overfitting failure mode. | governance | promotion-pipeline.md "What 'self-improving' means" | CAPTURED |
| Experiment/trial/promotion ledger schema | Sketch schema (experiment, trial_result, promotion_event, live_allocation tables) whose `status` field must record kills, not just winners, to keep N honest. | governance | promotion-pipeline.md "Ledger schema" | CAPTURED |
| 45-variant overfitting finding (lesson) | With five years of daily data, trying 45+ independent strategy variants makes it more likely than not that the best-selected strategy shows Sharpe ≥1.0 with zero true edge — the empirical basis for requiring the DSR gate. | validation | risk-and-failure.md §1 | CAPTURED |
| Lo (2002) Sharpe confidence-interval requirement | SE(Sharpe) formula shows a Sharpe-1.0 strategy needs ~2.7 years of daily data for 95% confidence it beats zero (optimistic i.i.d. case); real autocorrelated, fat-tailed crypto returns lengthen this further. | validation | risk-and-failure.md §2 | CAPTURED |
| Losing-streak kill-switch trap (lesson) | Binomial math shows a 10-loss streak at 55% win rate has non-trivial probability even in healthy strategies (17% by 1,000 trades); a naive "N consecutive losses" kill switch will shut down healthy strategies on normal variance. | risk | risk-and-failure.md §3 | CAPTURED |
| Fractional (half/quarter) Kelly position sizing | Continuous Kelly sizing is deliberately halved or quartered because overbetting past f* costs more growth than underbetting (concave growth curve) and full Kelly produces 50-60% drawdowns ~20% of the time over 1,000 bets; betting fraction c yields growth ≈ c(2−c) of maximum. | portfolio | risk-and-failure.md §4 / addendum "Kelly" | CAPTURED |
| Exchange-side dead man's switch / watchdog | A watchdog refreshes an exchange-side countdown (e.g., Kraken Dead Man's Switch, Binance countdownCancelAll, Coinbase Intl cancel-on-disconnect); if the strategy process hangs and stops refreshing, the exchange itself cancels resting orders independent of process state. | risk | risk-and-failure.md §5 | CAPTURED |
| Market-wide circuit-breaker reference | References US market-wide circuit breaker levels (L1=7%, L2=13%, L3=20% decline) as an external precedent for halt design. | risk | risk-and-failure.md §5 | CAPTURED |
| Knight Capital lesson: order-vs-intent and capital-threshold checks | Deploying dormant flag-repurposing code to only 7 of 8 servers with no order-vs-intent comparison, no capital threshold check and no kill switch caused $440-460M loss in 45 minutes — lesson is these checks must exist. | risk | risk-and-failure.md §6 | CAPTURED |
| Flash Crash lesson: price/time-sensitive execution logic | A volume-participation sell algorithm with no price or time sensitivity dumped $4.1B of E-minis in ~20 minutes, contributing to the 2010 Flash Crash and leading to Limit Up-Limit Down. | risk | risk-and-failure.md §6 | CAPTURED |
| MakerDAO lesson: dynamic gas settings under stress | Liquidation keeper bots ran static gas settings; a 10x gas spike stranded their bids and let bidders win collateral at $0, causing ~$5.67M protocol deficit — config fine in normal conditions became fatal in the exact conditions the bot was built for. | execution | risk-and-failure.md §6 | CAPTURED |
| BitMEX lesson: isolate ancillary features from critical infrastructure | A DDoS traced to an inefficient chat-feature query hitting the same database as the matching engine took the exchange offline 20-25 minutes during a crash. | operations | risk-and-failure.md §6 | CAPTURED |
| Oct 2025 Binance lesson: don't value collateral off an internal-only price feed | A targeted sell-off crashed Binance's internal quote to $0.65 while other venues stayed stable, contributing to $19.13B liquidated in 24h; lesson is collateral valuation needs external oracle cross-checks (attack-vs-design-flaw contested). | risk | risk-and-failure.md §6 | CAPTURED |
| Reconciliation: exchange as source of truth | Local state is rebuilt from the exchange on every startup before trading, since WebSocket feeds can drop silently, arrive out of order, or duplicate. | operations | risk-and-failure.md §7 | CAPTURED |
| Continuous REST polling as authoritative state | Continuous REST polling at 500ms-1s is treated as authoritative, with WebSocket used only as a fast-but-unverified hint. | operations | risk-and-failure.md §7 | CAPTURED |
| K-consecutive-mismatch gate before resync | Requires K consecutive mismatches before triggering a resync, to avoid thrashing on transient noise. | operations | risk-and-failure.md §7 | CAPTURED |
| Hard halt on failed startup reconciliation | Position quantity/average price must reconcile within tight tolerance (NautilusTrader invariant); if reconciliation fails at startup the system logs an error and refuses to start — hard halt beats trading unreconciled. | operations | risk-and-failure.md §7 | CAPTURED |
| Stale-feed age as an independent circuit-breaker signal | Feed staleness is itself monitored as a circuit-breaker trigger, independent of price movement — guards against "trading on a frozen order book." | risk | risk-and-failure.md §7 | CAPTURED |
| PSI drift threshold | Population Stability Index thresholds: <0.1 no shift, 0.1-0.2 moderate, ≥0.2 significant, used to monitor feature/prediction drift. | observability | risk-and-failure.md §8 | CAPTURED |
| KS test drift-monitoring caveat | KS test is flagged as too sensitive at scale (false positives on >100k rows at just 0.5% shift) and recommended only under ~1,000 rows. | observability | risk-and-failure.md §8 | CAPTURED |
| No magic-number promotion duration (lesson) | No authoritative "N months of paper trading" figure exists from any named source; the real criteria are DSR with honest N, CPCV regime coverage, permutation tests and walk-forward efficiency, then a 1-2 week minimum live size before scaling. | governance | risk-and-failure.md §9 | CAPTURED |
| Documented backtest-live performance gap | A real 3x leveraged-ETF rotation strategy underperformed backtest by ≈-2.7% total over 20 weeks live (-30.8 bps/week slippage, partly offset by +21.0 bps/week rounding), evidencing expected live decay. | validation | risk-and-failure.md §9 | CAPTURED |
| Liquidity does not predict slippage (lesson) | The higher-volume instrument sometimes had worse slippage due to rebalance crowding, so liquidity alone should not be assumed to predict execution quality. | execution | risk-and-failure.md §9 | CAPTURED |
| Wintermute lesson: never use vanity-address key generators | ~$160M loss traced to hot-wallet keys generated by the "Profanity" vanity-address tool, which had weak, brute-forceable entropy. | security | risk-and-failure.md addendum "More documented blow-ups" | CAPTURED |
| Mango Markets lesson: TWAP oracles and liquidity-scaled limits | $116M exploit from pumping a thin market and having an on-chain oracle read the manipulated spot price directly as collateral value; lesson is to use TWAP oracles and scale limits to real liquidity depth. | risk | risk-and-failure.md addendum "More documented blow-ups" | CAPTURED |
| Everbright lesson: idempotent retry + order-rate kill switch | An ETF-arb system repeatedly resubmitted orders on failure instead of stopping, producing ~$3.8B in erroneous orders; lesson is idempotent retry logic plus a hard order-rate kill switch. | risk | risk-and-failure.md addendum "More documented blow-ups" | CAPTURED |
| Halt policy: Flatten / Hold / Hedge modes | Flatten is default for system-integrity faults (stale data, wedged process, watchdog timeout); Hold is preferred for market-wide halts since forcing liquidation into an illiquid halted market can be worse than waiting; Hedge is a stopgap when direct unwinding is too costly. Halt logic must stay simple enough to execute correctly under degraded conditions. | risk | risk-and-failure.md addendum "Halt policy" | CAPTURED |
| Reconciliation-divergence response tiers | Within-tolerance mismatches auto-correct and log; beyond-tolerance mismatches alert a human; unexplained mismatches halt new order submission but do not necessarily force-close existing positions, since blind flattening on a bookkeeping fault can realise unnecessary losses. | operations | risk-and-failure.md addendum "Reconciliation on divergence" | CAPTURED |
| Regulatory quick-disable requirement | SEC Rule 15c3-5 and FINRA Notice 15-09 both require a "quick disable" capability built into algo trading software (principles-based, no numeric threshold). | governance | risk-and-failure.md addendum "Regulatory anchors" | CAPTURED |
| LULD price-band reference | Limit Up-Limit Down bands referenced as an external precedent: ±5%/±20% depending on price tier, doubling in the final 25 minutes, off a 5-min rolling reference. | risk | risk-and-failure.md addendum "Regulatory anchors" | CAPTURED |
| CPCV implementation with purge and embargo (reusable) | Prior attempt's `trading/strategy/cpcv.py` implements purge and embargo correctly with a sane combinatorial budget (n_groups=6, k_test=2); flagged as already-correct and worth reusing directly. | validation | prior-attempts-postmortem.md §2 Correction 2 / §5 | CAPTURED |
| Round-trip cost-charging structure (reusable, needs re-parameterisation) | Prior attempt's `backtest.py` correctly charges fee+slippage cost on both legs of a trade, but its default assumption (6bps round-trip) is ~3-4x too low versus real venue fees (Binance spot taker ~20bps round-trip) — dangerous because it looks rigorous while being wrong. | execution | prior-attempts-postmortem.md §3.1 / §5 | CAPTURED |
| Cost Engine as a first-class component, not a default constant (fix) | Cost parameters must be sourced from live fee schedules per venue as a dedicated Cost Engine component, never left as a hardcoded default argument. | operations | prior-attempts-postmortem.md §5 "Change" | CAPTURED |
| Synthetic-proxy validation substitution (anti-pattern) | Prior attempt trained/validated against a synthetic deterministic benchmark (Mackey-Glass chaos) because "crypto daily-direction is ~random and useless for development," reaching 97% accuracy on a proxy that doesn't resemble the real target; lesson is validation must use real market data with honest expectations, never a tractable synthetic proxy. | validation | prior-attempts-postmortem.md §3.3 / §5 | CAPTURED |
| Documentation-outrunning-validation anti-pattern | Markdown documentation grew ~200x (2→404 files, including a 928KB progress archive) across attempts while no record was found of any strategy that passed a gate and traded — inferred as effort concentrating where feedback is absent; fix is capped, indexed documentation. | build-process | prior-attempts-postmortem.md §3.2 / §5 | CAPTURED |
| Unmerged feature branches / versioned entry-point sprawl (anti-pattern) | Prior attempt's default branch was a never-merged feature branch, and entry points accumulated as new versioned files (run_brain.py, run_brain_agent.py, run_brain_t8.py, etc.) instead of replacing old ones; fix is one entry point, replaced not versioned, merged to a main line. | build-process | prior-attempts-postmortem.md §3.4 / §5 | CAPTURED |
| Restart-before-results anti-pattern | Six project attempts in twelve weeks meant no single attempt ran long enough to produce out-of-sample evidence justifying continuing or killing it; a strategy needs regime coverage, and so does a project. | build-process | prior-attempts-postmortem.md §4 | CAPTURED |
| No new repository without a gate-passing result or recorded abandonment reason (rule) | Explicit carried-forward rule: do not start a new repository until the current one has produced a gate-passing result or a documented reason for abandonment. | build-process | prior-attempts-postmortem.md §5 "Change" | CAPTURED |
| Prime directive: risk-managed income, not a lottery | Carried-forward verbatim goal from the prior project's brief: protect capital, take small risk-managed edges, compound — consistent income rather than lottery-style outcomes. | strategy | prior-attempts-postmortem.md §5 "Reuse directly" | CAPTURED |
| Dual-mode risk posture: unlimited paper, preservation-first live | Paper trading runs without restriction while live trading runs in a capital-preservation-first mode; carried forward as a correct pattern. | risk | prior-attempts-postmortem.md §5 "Reuse directly" | CAPTURED |
| CPU-first / GBT-beats-DL-at-these-horizons (supported finding) | Prior attempt's CPU-first, gradient-boosted-trees-on-engineered-features approach is independently supported by the research and carried forward as a default expectation, not an assumption. | models | prior-attempts-postmortem.md §5 "Reuse directly" | CAPTURED |
| Growing test-suite discipline (reusable habit) | Test file count grew 10→13→48→243 across attempts, a 24x increase read as a positive habit worth carrying forward. | build-process | prior-attempts-postmortem.md §2 Correction 3 / §5 | CAPTURED |
| Temporal Fusion Transformer (TFT) | Interpretable multi-horizon time-series forecaster; only finance use found is predicting realized volatility itself (no attached trading strategy/PnL) — verdict: backtest-paper evidence only, no live-trading evidence. | models | finml-architectures-live-evidence.md — section "1. Temporal Fusion Transformer (TFT)" | CAPTURED |
| N-BEATS | Point-forecast deep architecture (ICLR 2020), benchmarked on generic (non-finance) M3/M4/TOURISM data — verdict: not a finance-native architecture, backtest-paper evidence only. | models | finml-architectures-live-evidence.md — section "2. N-BEATS / N-HiTS" | CAPTURED |
| N-HiTS | Successor to N-BEATS (AAAI 2023), benchmarked on generic time-series sets — verdict: not finance-native, backtest-paper evidence only. | models | finml-architectures-live-evidence.md — section "2. N-BEATS / N-HiTS" | CAPTURED |
| Temporal Convolutional Network (TCN) | Convolutional sequence model applied to stock-market predictability (e.g. Hong Kong market study) — verdict: backtest-accuracy/return studies only, no live or paper-trading deployment reported. | models | finml-architectures-live-evidence.md — section "3. TCN (Bai, Kolter, Koltun)" | CAPTURED |
| S4 (Structured State Space model) | Long-sequence modeling architecture (ICLR 2022) — no finance-specific application found in this search; included as a candidate sequence model. | models | finml-architectures-live-evidence.md — section "4. State-space models: S4 / Mamba" | CAPTURED |
| Mamba (selective state-space model) | Linear-time sequence model; finance applications (MambaStock, Graph-Mamba, T-Mamba, CMDMamba) are thin, recent, mostly unreplicated 2024-2025 preprints — verdict: backtest-paper evidence only, thinner literature than TCN. | models | finml-architectures-live-evidence.md — section "4. State-space models: S4 / Mamba" | CAPTURED |
| Neural SDEs | Stochastic differential-equation-based generative/sequence models; finance-adjacent line targets derivatives pricing/hedging/calibration and market simulators, not directional trading signals — verdict: backtest/simulation evidence only, no live-trading claims. | models | finml-architectures-live-evidence.md — section "5. Neural SDEs" | CAPTURED |
| DeepLOB (CNN+LSTM on limit order books) | Predicts mid-price movement direction from raw LOB snapshots; SOTA on FI-2010 benchmark — but an independent 2024/2025 follow-up found "high forecasting power does not necessarily correspond to actionable trading signals" (accuracy doesn't survive round-trip executability/profitability) — verdict: most finance-native architecture reviewed, but no live-trading evidence and a direct rebuttal of accuracy-equals-edge. | models | finml-architectures-live-evidence.md — section "6. DeepLOB" | CAPTURED |
| Fractional differentiation | Generalizes integer differencing to a real-valued order d in [0,1] via a weighted sum of past values (Fixed-Width Window variant), binary-searching the minimum d that passes an ADF stationarity test — preserves more memory/signal than naive returns while still achieving stationarity. Production-usable via the `fracdiff` PyPI package; `mlfinlab`'s public implementation is stub-only/paid-gated. | feature-engineering | finml-feature-engineering.md — section "1. Fractional differentiation" | CAPTURED |
| Triple-barrier labeling | Labels training examples by whichever of three barriers (profit-take, stop-loss, max holding period), scaled to rolling volatility, is touched first — reflects realizable trade outcomes under real risk management instead of naive fixed-horizon sign-of-return labels. | feature-engineering | finml-feature-engineering.md — section "2. Triple-barrier labeling and meta-labeling" | CAPTURED |
| Meta-labeling | Secondary model that decides bet size / whether to act on a primary directional model's call, decoupling recall (primary) from precision (secondary) — improves Sharpe/Calmar/drawdown even without improving raw hit rate. Caveat: independent (non-Hudson & Thames) validation of the magnitude of improvement is weak — the widely-cited 20%→77%/17%→63% accuracy figures come from a commercially-interested party's blog post, not a peer-reviewed independent study. | strategy | finml-feature-engineering.md — section "2. Triple-barrier labeling and meta-labeling" | CAPTURED |
| Volatility-based features (realized vol, GARCH, HAR-RV) | Realized volatility / GARCH-family / HAR-RV features; HAR models on high-frequency realized variance generally beat GARCH-family for short-term crypto volatility forecasting OOS — the most consistently useful, least controversial feature family reviewed. | feature-engineering | finml-feature-engineering.md — section "3. Feature families with credible documented edge" | CAPTURED |
| Order flow imbalance (OFI), microstructure level | Imbalance of order flow at best bid/ask; robust, independently-replicated linear relationship to short-horizon price changes across 50 NYSE stocks (Cont-Kukanov-Stoikov). One of the most independently-replicated microstructure findings, though the core citation is equities, not crypto. | feature-engineering | finml-feature-engineering.md — section "4. Crypto/microstructure OFI specifically" | CAPTURED |
| Cross-sectional / relative momentum-reversal features | Momentum/reversal (Jegadeesh-Titman style) cross-sectional features — credible by broad, long-established academic consensus, though not re-verified with a live citation in this research pass. | feature-engineering | finml-feature-engineering.md — section "3. Feature families with credible documented edge" | CAPTURED |
| Technical indicator zoo (RSI/MACD/Bollinger/chart patterns) | Combinatorial library of classic technical indicators — strong critical literature (Sullivan/Timmermann/White 1999; Park/Irwin 2007) shows apparent edge from large indicator/rule search vanishes once corrected for data-snooping — verdict: don't build a large technical-indicator library. | feature-engineering | finml-feature-engineering.md — section "3. Feature families with credible documented edge" | CAPTURED |
| Macro FX order-flow cross-asset signal | International FX order flow across 11 major currencies predicting the cross-section of crypto returns at daily/weekly horizons via nonlinear ML — found genuine OOS predictive power, dominates fundamentals-based benchmarks, but is a macro/cross-asset signal, distinct from tick-level LOB imbalance. | feature-engineering | finml-feature-engineering.md — section "4. Crypto/microstructure OFI specifically" | CAPTURED |
| Gradient Boosted Trees (GBT/XGBoost/LightGBM) | Tree-boosting model family; wins outright at daily/statistical-arbitrage frequency (Krauss et al., beats DNN) and is Numerai's documented default/benchmark tool (crushes linear regression, Sharpe 0.91 vs 0.13) — but edges DL only narrowly or not at all at monthly horizon (NN3 R²=0.40% vs GBRT 0.34%) and mixed at LOB microstructure. Verdict: frequency-dependent — strong default for daily-and-lower frequencies. | models | finml-gbt-vs-dl-by-frequency.md — section "3. Numerai" and "5. Lower-frequency band" | CAPTURED |
| Random Forest | Tree-ensemble model; best individual performer in the one apples-to-apples daily-frequency three-way comparison found (Krauss et al.: RF 0.43%/day, Sharpe 5.12, beating both GBT and DNN, before and after transaction costs). | models | finml-gbt-vs-dl-by-frequency.md — section "5. Lower-frequency band (hours-to-minutes): strongest evidence for trees" | CAPTURED |
| Shallow feed-forward NN depth selection (NN1-NN5, optimal at 3 layers) | Feed-forward network for monthly stock-return prediction; performance peaks at 3 hidden layers (NN3, R²=0.40%) and degrades at NN4/NN5 — deeper is not better for this task. | models | finml-gbt-vs-dl-by-frequency.md — section "1. Gu, Kelly, Xiu (2020)" | CAPTURED |
| Hybrid ES-RNN (exponential smoothing + LSTM) | Combines classical exponential smoothing with an LSTM; won the M4 forecasting competition outright — deep-learning-based, not GBT. Demand-forecasting context, not directly validated on financial returns. | models | finml-gbt-vs-dl-by-frequency.md — section "2. M4 and M5 (Makridakis et al.)" | CAPTURED |
| FFORMA (XGBoost meta-learner for forecast combination) | Uses XGBoost not as the forecaster but as a meta-learner assigning combination weights across 9 classical statistical methods (ARIMA, ETS, TBATS, etc.); placed 2nd in M4. | models | finml-gbt-vs-dl-by-frequency.md — section "2. M4 and M5 (Makridakis et al.)" | CAPTURED |
| Model ensembling / combining multiple model classes | Combining diverse model classes (M5's winning 6-model equal-weighted ensemble; Numerai's Meta Model, Sharpe 1.55 vs single-model best) generally beats single-model-class approaches — though not universally: in Krauss et al., a single model (Random Forest, Sharpe 5.12) beat the 3-way ensemble (Sharpe 4.71). | models | finml-gbt-vs-dl-by-frequency.md — section "2. M4 and M5" and "3. Numerai" | CAPTURED |
| Dataset-characteristics-based NN-vs-GBDT selection heuristic | Whether NN or GBDT wins on tabular data is predicted by dataset size, size-to-feature ratio, and "irregularity" (McElfresh et al., NeurIPS 2023, 176 datasets) — the gap is negligible on most datasets; use dataset characteristics rather than a blanket preference to choose. | models | finml-gbt-vs-dl-by-frequency.md — section "4. Where DL genuinely earns its keep" | CAPTURED |
| DL-as-feature-extractor feeding a downstream GBT (e.g. FinBERT sentiment → XGBoost) | Pattern where a deep model handles unstructured-text representation learning and a GBT makes the final prediction (FinBERT sentiment features into XGBoost beat technical-only/lexicon-only baselines) — no paper found where end-to-end DL beats this hybrid pattern for NLP/alt-data prediction. | feature-engineering | finml-gbt-vs-dl-by-frequency.md — section "4. Where DL genuinely earns its keep" | CAPTURED |
| Financial text embeddings as direct return predictors | Using embeddings (OpenAI text-embedding-3-large, FinBERT, DeBERTa) directly as predictors of stock returns; best Sharpe ~7.6% per prediction in a Stanford class project — modest, not benchmarked against a GBT-on-engineered-features baseline, so does not establish DL superiority. | feature-engineering | finml-gbt-vs-dl-by-frequency.md — section "4. Where DL genuinely earns its keep" | CAPTURED |
| FinBERT (domain-adapted financial sentiment model) | BERT further pretrained on financial corpora and fine-tuned on financial sentiment labels; consistently outperforms generic BERT/generic sentiment models on financial text because financial language ("beat," "miss," "guidance") carries different polarity — verdict: use an existing domain-adapted model like FinBERT (open-weight, zero training cost) rather than a generic sentiment model or building your own from scratch. | intelligence | finml-subsecond-and-dontbuild.md — section "B4. Generic pretrained sentiment models" | CAPTURED |
| Deflated Sharpe Ratio (DSR) | Corrects the classical Sharpe-ratio significance test for selection bias under multiple testing (picking the best of N trials) and non-normality (skewness/kurtosis) — compares observed Sharpe to an expected chance-maximum Sharpe; makes raw backtested Sharpe after searching many variants near-meaningless without this correction. | validation | finml-replication-problem.md — section "3. Deflated Sharpe Ratio" | CAPTURED |
| Probability of Backtest Overfitting (PBO) / Combinatorially Symmetric Cross-Validation (CSCV) | Methodology (Bailey/Borwein/Lopez de Prado/Zhu) that estimates the probability a given backtest is overfit via combinatorial cross-validation — a tool, not an empirical claim about how much of the industry is overfit. | validation | finml-replication-problem.md — section "2. Lopez de Prado's backtest-overfitting critique" | CAPTURED |
| White's Reality Check | Bootstrap test for whether the *best* of many candidate trading rules/models genuinely beats a benchmark, correcting for multiple comparisons — foundational data-snooping-bias test. | validation | finml-replication-problem.md — section "4. Data-snooping / reality-check literature" | CAPTURED |
| Hansen's Superior Predictive Ability (SPA) test | Refinement of White's Reality Check that is more powerful/less conservative by better handling poor/irrelevant models in the comparison set. | validation | finml-replication-problem.md — section "4. Data-snooping / reality-check literature" | CAPTURED |
| Stricter significance hurdle (t>3.0) for factor/anomaly discovery | Given hundreds of tested factors, Harvey-Liu-Zhu argue the conventional t>2.0 threshold is too lenient and propose t>3.0 — most claimed findings are statistically indistinguishable from false positives under the old threshold (disputed by Chen 2022, who argues 75-91% of findings are likely true). | validation | finml-replication-problem.md — section "1. Quantifiable replication/decay rates" | CAPTURED |
| NYSE-breakpoint / value-weighted anomaly re-testing methodology | Re-testing published anomalies using NYSE breakpoints (not all-exchange) and value-weighted (not equal-weighted) returns to remove distortion from tiny illiquid microcaps — Hou/Xue/Zhang found ~64% (t>1.96) to ~85% (t>3) of 447 anomalies become insignificant under this correction. | validation | finml-replication-problem.md — section "1. Quantifiable replication/decay rates" | CAPTURED |
| Multiple-testing correction for parameter sweeps / walk-forward optimization | Any pipeline doing parameter sweeps, walk-forward optimization, or multi-signal search should run a multiple-testing correction (Reality Check, SPA test, DSR, or PBO/CSCV) rather than reporting the single best backtest's raw Sharpe. | validation | finml-replication-problem.md — section "4. Data-snooping / reality-check literature" | CAPTURED |
| LLM lookahead-bias check for LLM-derived signals | LLMs trained on historical text can "know" outcomes within their training window, contaminating backtests evaluated on dates inside that window ("lookahead propensity") — an emerging, not-yet-fully-solved trap specific to using any LLM-based signal. | validation | finml-replication-problem.md — section "5. Documented cases of famous results failing in practice" | CAPTURED |
| Crowding / correlated-strategy risk monitoring | August 2007 quant-quake: individually-robust-looking, similarly-constructed strategies (value/contrarian factors) blew up together live via a forced-deleveraging cascade ("Unwind Hypothesis") — backtests that don't model crowding miss a structural blind spot distinct from overfitting, directly relevant given how correlated many public crypto strategies already are. | risk | finml-replication-problem.md — section "5. Documented cases of famous results failing in practice" | CAPTURED |
| Region-matched cloud VM co-located with exchange's own cloud infrastructure | Renting a VM in the same cloud region as the exchange's own hosting (Binance=AWS Tokyo, Bybit=AWS Singapore, OKX=AWS Hong Kong) achieves single-digit-to-teens-ms RTT without colocation, for roughly $250-300/mo — a legitimate, cheap way to reduce latency vs. a generic/wrong-region cloud VM (15-25ms). | execution | finml-subsecond-and-dontbuild.md — section "A3. The realistic middle ground for a small/independent shop" | CAPTURED |
| AWS Local Zones | Purchasable cloud product (e.g. NYC-2A) that gets standard cloud tooling to ~1.5-2ms end-to-end from an 8-12ms public-cloud baseline — roughly colo-adjacent latency without full colocation cost/complexity. | execution | finml-subsecond-and-dontbuild.md — section "A1. Latency: cloud VM vs colocation" | CAPTURED |
| Exchange-offered colocation (Kraken, Deribit) | Kraken launched retail-accessible colocation in 2025 (sub-millisecond target); Deribit offers LD4 London colocation — Binance explicitly declined colocation and Coinbase shut down its HFT-colocation effort. Relevant only if a strategy's edge genuinely depends on sub-millisecond reaction; a distinct, much more expensive build to scope separately. | execution | finml-subsecond-and-dontbuild.md — section "A3. The realistic middle ground for a small/independent shop" | CAPTURED |
| Microsecond latency-arbitrage HFT market making | Competing on microsecond-scale latency races under continuous limit-order-book market design (races last 5-10 microseconds, ~20% of volume per Aquilina/Budish/O'Neill LSE study) — verdict: don't build; categorically unreachable from any cloud VM (8ms+ minimum), and no credible academic or practitioner source claims small/non-colocated shops can compete here. | strategy | finml-subsecond-and-dontbuild.md — section "A2. Consensus against small/retail shops competing at true HFT latency" | CAPTURED |
| Sub-second execution tier scoping (single-digit-to-tens-of-ms band) | Defined, defensible latency tier for a cloud-based shop — region-matched VM + exchange WebSocket/FIX feed + application-level latency discipline — explicitly scoped as "not latency arbitrage," nowhere near true HFT (microseconds). | execution | finml-subsecond-and-dontbuild.md — section "A3. The realistic middle ground for a small/independent shop" | CAPTURED |
| LSTM/RNN on raw past price → future price (no engineered features) | Predicting future price directly from raw price history with no feature engineering; adjacent evidence (a single-layer linear model, DLinear, beats Transformer/LSTM-class models on standard long-term forecasting benchmarks) supports the trap that such models degenerate to a lag-1 "predict-the-last-price" predictor — verdict: don't bother; self-check against a naive last-price baseline. | models | finml-subsecond-and-dontbuild.md — section "B1. LSTM/RNN on raw past price -> future price, no engineered features" | CAPTURED |
| GANs for synthetic financial data as an edge source | Generating synthetic price/order-book data (Quant-GAN, Stock-GAN, TimeGAN-style) evaluated on statistical-fidelity metrics — no source found claiming a GAN-generated-data pipeline delivered real live trading edge over simpler approaches — verdict: don't build this as an "edge" project; it's a research curiosity, not a shortcut to edge. | feature-engineering | finml-subsecond-and-dontbuild.md — section "B2. GANs for synthetic financial data as a source of trading edge" | CAPTURED |
| Block bootstrap / regime-conditional resampling | Alternative to GANs for genuine data-scarcity problems (thin order-book history, new listing) — cheaper and just as defensible as synthetic-data generation. | feature-engineering | finml-subsecond-and-dontbuild.md — section "B2. GANs for synthetic financial data as a source of trading edge" | CAPTURED |
| End-to-end RL for execution/strategy without heavy reward-shaping | Naive top-level "learn to trade" reinforcement learning; RL's core premise (delayed/sparse reward, agent affects environment) is structurally mismatched to trading (near-zero market impact, immediate reward), prone to reward hacking against simulator artifacts, and severely sample-inefficient — verdict: don't build naive end-to-end RL as the top-level system. | strategy | finml-subsecond-and-dontbuild.md — section "B3. End-to-end RL for execution/strategy without heavy reward-shaping/feature engineering" | CAPTURED |
| Constrained RL for execution scheduling (sub-problem) | If RL is used at all, it belongs inside a heavily constrained, reward-shaped sub-problem such as execution scheduling within a strategy that has already decided direction/size via other means — not as the top-level trading decision. | execution | finml-subsecond-and-dontbuild.md — section "B3. End-to-end RL for execution/strategy without heavy reward-shaping/feature engineering" | CAPTURED |
| Generic (non-domain-adapted) pretrained sentiment models | Off-the-shelf, non-finance-tuned sentiment models applied to financial text — verdict: don't bother; financial language carries different polarity than general-domain text, and a domain-adapted alternative (FinBERT) is available at zero training cost. | feature-engineering | finml-subsecond-and-dontbuild.md — section "B4. Generic pretrained sentiment models (no domain adaptation) on financial text" | CAPTURED |
| Wavelet transforms / exotic signal processing on price series | Applying wavelet decomposition as a feature-engineering step; base technique is prone enough to overfitting that papers bolt on extra feature-selection machinery to contain it, and standard non-causal wavelet implementations can leak future-sample information across window boundaries unless a strictly causal streaming decomposition is used — verdict: don't build this as an early-stage investment; late-stage only, with an explicit causality unit test. | feature-engineering | finml-subsecond-and-dontbuild.md — section "B5. Wavelet transforms / exotic signal processing on price series" | CAPTURED |
| Exotic/custom loss functions as a substitute for fixing labels/features | Reaching for a custom loss function before verifying labels/features aren't the actual bottleneck — weaker evidence than the other "don't build" items (general ML folklore, not trading-specific peer-reviewed consensus), but directionally right: verify a standard loss with fixed data first before ruling in a custom loss. | models | finml-subsecond-and-dontbuild.md — section "B6. Exotic/custom loss functions as a substitute for fixing labels/features" | CAPTURED |
| Naive-baseline MSE check (predict-last-observed-price) | Cheap self-check to detect the LSTM/RNN lag-1 trap: compare a trained model's held-out MSE against a naive "predict last observed price" baseline — if within noise, the model has reproduced the trap. | validation | finml-subsecond-and-dontbuild.md — section "B1. LSTM/RNN on raw past price -> future price, no engineered features" | CAPTURED |
| Kronos foundation model | Decoder-only autoregressive transformer over K-line (OHLCV) sequences, pretrained across 45+ exchanges, with LLM-shaped probabilistic sampling inference (top-k/top-p, temperature, multi-sample averaging) — verdict: use as the baseline your own models must beat, not as a signal source; it models zero trading costs and its train/val/test splits overlap with no purging/embargo, so its reported numbers don't transfer directly to live use. | models | kronos-foundation-model.md — section "What it actually is" and "Two findings that decide how it can be used" | CAPTURED |
| Binary Spherical Quantization / hierarchical discrete tokenization of OHLCV | Quantizes continuous OHLCV into hierarchical discrete tokens (coarse + fine tiers) via BSQuantizer inside an encoder/decoder transformer — an independently interesting representation-learning contribution worth understanding even if the full Kronos model is only ever used as a baseline. | feature-engineering | kronos-foundation-model.md — section "What it actually is" | CAPTURED |
| Zero-shot foundation-model baseline gating | Running a zero-shot foundation model (e.g. Kronos-small) through your own cost engine and purged/embargoed validation harness as a decisive, cheap test of whether your own pipeline adds value — if your pipeline can't beat an off-the-shelf model that never saw your data, that's a decisive finding before building anything expensive. | validation | kronos-foundation-model.md — section "How to use it here" | CAPTURED |
| Trial Registry (register searched/fine-tuned models as trials) | Any fine-tuned or searched candidate model (e.g. a fine-tuned Kronos) must be counted in the Trial Registry like any other trial, since fine-tuning on your own data makes it a searched candidate, not a clean zero-shot baseline. | governance | kronos-foundation-model.md — section "How to use it here" | CAPTURED |
| Transaction Cost Engine (commission/slippage/fee modeling) | Modeling round-trip transaction costs (Binance round-trip breakeven ~22-30bps, with fees dominating slippage/adverse selection by roughly 5-10x at typical order sizes) in any backtest — a backtest with no cost model (like Kronos's bundled backtest, verified via repo grep to have zero cost-related code) measures only half the profitability question. | risk | kronos-foundation-model.md — section "Two findings that decide how it can be used — 1. It models no trading costs whatsoever" | CAPTURED |
| Purged/embargoed cross-validation (CPCV) | Validation harness that purges/embargoes overlapping label windows around train/val/test split boundaries — the failure mode Kronos's own splits exhibit (val begins ~4 months before train ends, test begins ~3 months before val ends, with a 120-candle prediction horizon straddling those boundaries and no purge/embargo) is exactly what this technique is meant to prevent. | validation | kronos-foundation-model.md — section "Two findings that decide how it can be used — 2. Its train/val/test splits deliberately overlap" | CAPTURED |
| FunSearch | LLM generates candidate program mutations scored by a deterministic evaluator, with an "islands" program-database maintaining diverse evolutionary sub-populations; peer-reviewed (Nature 2023/2024), LLM weights never change. Found a new cap-set lower bound and improved online bin-packing heuristics — code partially open (excludes the LLM sampler and sandbox). | build-process | llm-code-evolution-funsearch-alphaevolve-adas.md — section "1. FunSearch" | CAPTURED |
| AlphaEvolve | Gemini-powered coding agent that evolves algorithms (matrix multiplication, data-center scheduling, chip design, kernel optimization); NOT peer-reviewed — a self-labeled company "white paper" with results self-reported by Google and mostly unaudited (one output, a 4x4 complex matrix-multiplication algorithm, was independently verified). Not open source. | build-process | llm-code-evolution-funsearch-alphaevolve-adas.md — section "2. AlphaEvolve" | CAPTURED |
| ADAS (Automated Design of Agentic Systems / Meta Agent Search) | A meta-agent iteratively writes new agent architectures as code (prompts, tool use, control flow), each evaluated on benchmark tasks with results feeding a growing archive; peer-reviewed (ICLR 2025). Best discovered agents beat hand-designed baselines by 14.4% accuracy on MGSM math. Authors themselves flag that sandboxing/manual review is required since agents are arbitrary self-modifying code. | build-process | llm-code-evolution-funsearch-alphaevolve-adas.md — section "3. ADAS" | CAPTURED |
| Eureka | LLM (GPT-4) evolves RL reward-function code, with fitness measured by a full RL training run in Isaac Gym; peer-reviewed (ICLR 2024), open source. Beat human-engineered rewards on 83% of 29 tasks, average +52%. | build-process | llm-code-evolution-funsearch-alphaevolve-adas.md — section "4. Other comparable systems" | CAPTURED |
| STOP (self-improving LLM scaffold) | LLM-scaffolded "improver" recursively improves its own code via beam search/GA/annealing; authors note this is not true recursive self-improvement since LLM weights never change. Open source, peer-review venue unverified. | build-process | llm-code-evolution-funsearch-alphaevolve-adas.md — section "4. Other comparable systems" | CAPTURED |
| EvoPrompt | Genetic-algorithm/differential-evolution search over discrete natural-language prompts; up to +25% over human-written prompts on BIG-Bench-Hard. Peer-review venue unverified. | build-process | llm-code-evolution-funsearch-alphaevolve-adas.md — section "4. Other comparable systems" | CAPTURED |
| OpenEvolve | Unofficial, actively maintained open-source reimplementation of the AlphaEvolve pattern (not Google-affiliated), supporting API-based or local LLMs; claims to match published circle-packing results (self-reported, not independently audited) — the most realistic starting point for a small team wanting an AlphaEvolve-shaped loop. | build-process | llm-code-evolution-funsearch-alphaevolve-adas.md — section "4. Other comparable systems" | CAPTURED |
| ShinkaEvolve | Independent group's open-ended program-evolution system that exceeded AlphaEvolve's own circle-packing (n=26) result — genuine evidence an outside team extended one narrow AlphaEvolve result independently. | build-process | llm-code-evolution-funsearch-alphaevolve-adas.md — section "4. Other comparable systems" | CAPTURED |
| Trading-specific LLM-evolution strategy search (MadEvolve, QuantaAlpha, EVOQUANT, and similar hobby repos) | Applying the AlphaEvolve/evolutionary-code pattern to generate trading strategies/alpha factors — small cluster of unreviewed 2026 preprints and hobbyist GitHub repos, no disclosed walk-forward/robustness methodology in most cases — verdict: active experimentation, but unvalidated extrapolation, not a demonstrated capability; no peer-reviewed or independently replicated evidence of genuine OOS trading edge. | build-process | llm-code-evolution-funsearch-alphaevolve-adas.md — section "5. Has this been applied to trading in a peer-reviewed or credibly-verified setting? — NO" | CAPTURED |
| Quality-diversity / archive-based search for evolutionary code discovery | Recurring mechanism across FunSearch (program-database "islands") and ADAS (growing archive conditioning future proposals) — maintains diverse parallel candidate populations and periodically cross-pollinates top performers, structurally similar to MAP-Elites-style open-ended search. | build-process | llm-code-evolution-funsearch-alphaevolve-adas.md — section "1. FunSearch" and "3. ADAS" | CAPTURED |
| Fitness-function prerequisite checklist before applying evolutionary code search to trading | Every demonstrated success of this class of system (FunSearch, AlphaEvolve, ADAS, Eureka) requires a fast/cheap-to-evaluate, low-noise/deterministic, construct-valid, stationary, well-bounded fitness function — backtested trading fitness (Sharpe/PnL) is noisy, non-stationary, gameable via the same massive-search mechanism that makes these systems powerful (manufacturing backtest overfitting), expensive to genuinely scale, and has a construct-validity gap (high backtested Sharpe is a hypothesis, not proof) — verdict: trading fitness violates nearly all five prerequisites. | build-process | llm-code-evolution-funsearch-alphaevolve-adas.md — section "6. THE CRUX: fitness-function prerequisites and why trading breaks them" | CAPTURED |
| Sandboxed execution + manual review for self-modifying agent-generated code | ADAS authors' own required mitigation: containerized/sandboxed execution and manual review of LLM-generated agent code, since agents are arbitrary self-modifying code — directly analogous to any system letting an LLM write and auto-execute trading logic. | security | llm-code-evolution-funsearch-alphaevolve-adas.md — section "3. ADAS" | CAPTURED |
| Slippage/market-impact-vs-size modeling | Estimates bps slippage as a function of order notional size, cross-venue; only sparse/qualitative data exists for 2025-2026 conditions (BTC less slippage than ETH at every size tested). | market-data | crypto-alpha-decay-execution-costs.md — section 1, Slippage/market impact magnitudes | CAPTURED |
| Adverse-selection cost estimation (spread decomposition) | Uses Huang-Stoll/Madhavan-style decomposition to measure the adverse-selection share of the effective spread; BTC measured at ~7% of spread (lowest of 12 coins studied), translating to ~0.1-0.3bps per resting-limit-order fill on majors. | feature-engineering | crypto-alpha-decay-execution-costs.md — section 2, Adverse selection / queue position | CAPTURED |
| Round-trip breakeven cost calculator | Sums fees + slippage + adverse selection to derive a total breakeven bps threshold per trade; found to be ~10-30bps on Binance base/VIP tiers vs ~125-135bps on Coinbase retail tier, dominated by fee schedule not microstructure. | execution | crypto-alpha-decay-execution-costs.md — section 3, Round-trip breakeven bps | CAPTURED |
| TWAP/VWAP/Almgren-Chriss optimal execution slicing | Order-slicing/participation algorithms that trade off market impact vs timing risk; verdict for this account size ($1k-$100k clips on BTC/ETH) is it does NOT matter — clip size is 5-10 thousand times below where execution-algo theory starts to bite. | execution | crypto-alpha-decay-execution-costs.md — section 4, Where TWAP/VWAP/Almgren-Chriss start to matter | CAPTURED |
| Participation-rate (% of ADV) order-size throttle | Industry heuristic capping a single clip at ~1% of ADV and POV algos at 5-10% of concurrent volume; noted as practitioner convention, not peer-reviewed, and irrelevant at this account's size except on illiquid alts or single clips above ~$200k-$500k in stressed conditions. | risk | crypto-alpha-decay-execution-costs.md — section 4 | CAPTURED |
| Survivorship-bias-aware backtesting | Backtests must account for delisted coins vanishing from datasets, which inflates apparent returns; specific inflation magnitudes found in sources were rejected as unverifiable marketing claims. | validation | crypto-alpha-decay-execution-costs.md — section 5, Backtesting traps | CAPTURED |
| Outage-aware backtest fill assumptions | Backtests must not assume fills during exactly the volatile windows exchanges historically go down (verified recurring pattern, e.g. Oct 10 2025, Coinbase AWS outage) — any backtest assuming fills then is fiction. | validation | crypto-alpha-decay-execution-costs.md — section 5 | CAPTURED |
| Look-ahead bias avoidance on close prices | Avoid using vendor-blended/VWAP-smoothed "close" prices that were never tradable on any single venue at that instant. | validation | crypto-alpha-decay-execution-costs.md — section 5 | CAPTURED |
| Flat/static slippage-assumption check | Flags that a flat bps slippage assumption (e.g. "5bps always") in a backtest is almost certainly wrong in both directions given how thin real bps-vs-size data is. | validation | crypto-alpha-decay-execution-costs.md — section 5 | CAPTURED |
| Exchange-downtime-during-volatility risk awareness | Documents that every major volatility spike since 2020 has a matching exchange degradation event on at least one top-5 venue (May 2021, Jan 2021, May 2022, Nov 2022, Feb/Mar 2020, Oct 2025) — treated as close to a base rate, not tail risk. | risk | crypto-counterparty-operational-risk.md — section 1, Exchange downtime during high volatility | CAPTURED |
| Per-exchange API rate-limit compliance | Tracks documented rate limits (Binance weight-based, Bybit 600 req/5s, OKX per-endpoint, Coinbase per-IP/profile, Kraken decaying counters) and their ban/lockout consequences (e.g. Binance HTTP 418 auto-ban scaling 2min-3days). | operations | crypto-counterparty-operational-risk.md — section 2, API rate limits and ban policy | CAPTURED |
| IP/UID-shared rate-limit budget awareness | Notes that Binance and Bybit rate limits are IP-shared, not per-strategy — a burst from one strategy on a multi-strategy bot can lock out all strategies on that exchange. | risk | crypto-counterparty-operational-risk.md — section 2, Implication for a multi-frequency system | CAPTURED |
| Cancel/amend order-churn throttling awareness | Notes that Kraken and OKX rate-limit systems explicitly penalize cancel/amend churn more heavily than new orders. | execution | crypto-counterparty-operational-risk.md — section 2 | CAPTURED |
| Withdrawal-freeze risk monitoring | Tracks historical cases (OKEx 5.5-week freeze from key-holder detention, Binance 3.5hr freeze from stuck tx, Bybit freeze from third-party Safe{Wallet} infra failure during Feb 2025 hack) as a category of counterparty risk distinct from insolvency/hacks. | risk | crypto-counterparty-operational-risk.md — section 3, Withdrawal freezes | CAPTURED |
| Proof-of-Reserves skepticism | Treats exchange Proof-of-Reserves as a necessary-but-insufficient signal — it's a single-day snapshot of assets from the exchange's own ledger, doesn't verify liabilities or "window dressing," and has documented implementation flaws (weak hashing, summation-tree manipulation). | risk | crypto-counterparty-operational-risk.md — section 4, Post-FTX counterparty risk management | CAPTURED |
| Per-exchange capital caps / diversification | Caps any single exchange at roughly 25-30% of total capital, requiring 4+ venues, to bound single-counterparty tail loss. | risk | crypto-counterparty-operational-risk.md — section 5, Practical recommendation | CAPTURED |
| Scheduled auto-sweep to self-custody | Sweeps balance above a rolling threshold (e.g. anything above 110% of trailing 7-day peak margin usage) from exchange to self-custody on a daily/weekly schedule, rather than zero-balance-at-rest (deemed unworkable for an active strategy). | operations | crypto-counterparty-operational-risk.md — section 5 | CAPTURED |
| Institutional custody cost-benefit assessment | Evaluates qualified custodians (Coinbase Prime, BitGo, Fireblocks, Anchorage) for this account size; verdict: not cost-effective at $100K-$1M (fee drag several hundred bps/yr or below minimum) — economics only work at roughly $5M+. | governance | crypto-counterparty-operational-risk.md — section 4/5 | CAPTURED |
| Self-custody via multisig/hardware wallet | Recommended off-exchange holding mechanism at this account size (e.g. Gnosis Safe or hardware wallet) instead of institutional custody, with the caveat that Safe itself was implicated in the Bybit incident and isn't risk-free. | security | crypto-counterparty-operational-risk.md — section 5 | CAPTURED |
| Rate-limit headroom buffer | Builds API clients to operate under 50% of documented rate limits during normal operation, specifically because the ban/lockout is IP/UID-wide and can silence all strategies on a venue. | operations | crypto-counterparty-operational-risk.md — section 5 | CAPTURED |
| Circuit breaker on API errors / price deviation | Halts new orders on a venue if its API returns elevated error rates or its price deviates >X% from a reference/oracle feed, rather than blindly retrying into a degraded matching engine. | risk | crypto-counterparty-operational-risk.md — section 5 | CAPTURED |
| Maker/taker fee-tier tracking across exchanges | Tracks each exchange's base and VIP-tier maker/taker fee schedules (Binance 0.10%/0.10% base, Bybit 0.1%/0.1% spot flat, Coinbase ~0.40%/0.60% base, Kraken 0.40%/0.80% base) to select venue/tier for cost. | operations | crypto-exchange-fees-maker-economics.md — Binance/Bybit/OKX/Coinbase/Kraken sections | CAPTURED |
| Native-token fee discount (e.g. BNB) | Applies a native-token holding/payment discount (Binance: 25% off at every tier when paid in BNB) to reduce effective fees. | execution | crypto-exchange-fees-maker-economics.md — Binance section | CAPTURED |
| Post-only order type | Uses each exchange's documented post-only order flag (Binance LIMIT_MAKER, Bybit PostOnly, OKX post_only, Coinbase post_only, Kraken post) which is rejected rather than executed as taker if it would cross the book. | execution | crypto-exchange-fees-maker-economics.md — Post-only mechanics | CAPTURED |
| Maker-rebate / liquidity-provision strategy | Standalone rebate-capturing market-making strategy; verdict: NOT viable at this account size from a cloud VM — no true rebate is reachable at this volume, and the thin maker/taker differential is competed away by colocated makers via adverse selection. | strategy | crypto-exchange-fees-maker-economics.md — Direct answer on viability | CAPTURED |
| Post-only as cost-reduction (not rebate-farming) | Recommends using post-only limit orders on trades already intended (directional conviction, can tolerate non-fill) purely to save the maker/taker spread, not as a standalone liquidity-provision strategy. | execution | crypto-exchange-fees-maker-economics.md — Practical recommendation | CAPTURED |
| Route via Advanced Trade/API, not retail app | Avoids routing automated flow through Coinbase's retail app pricing (effective 1-4% all-in), always using Advanced Trade/Exchange API pricing instead. | execution | crypto-exchange-fees-maker-economics.md — Coinbase section | CAPTURED |
| Cloud-region selection for exchange proximity | Selects cloud VM region to minimize RTT to a given exchange's matching engine (e.g. AWS Tokyo for Binance ~23ms vs us-east-1 ~424ms; Bybit fastest from Singapore, Kraken from Frankfurt). | operations | crypto-latency-cloud-vs-colo.md — section 1 | CAPTURED |
| Passive market-making for queue priority (CEX) | Resting orders to win queue priority at best bid/ask; verdict: don't bother from a cloud VM — colocated firms operate at ~1-3ms vs cloud VM 20-500ms, losing queue priority on effectively every contested tick. | strategy | crypto-latency-cloud-vs-colo.md — section 4 | CAPTURED |
| Latency arbitrage (cross-exchange/spot-perp) | Racing price discrepancies across venues on sub-tick timescales; verdict: don't bother — structurally 10-100x slower than colocated bots from a cloud VM. | strategy | crypto-latency-cloud-vs-colo.md — section 4 | CAPTURED |
| Statistical arb / pairs trading (sub-second-to-few-second) | Verdict: degraded but not flatly closed if holding horizon is toward the multi-second end and the VM is in the correct region; sub-second horizon converges to the latency-arb case and should be abandoned. | strategy | crypto-latency-cloud-vs-colo.md — section 4 | CAPTURED |
| Momentum/signal trading (minute-to-hour horizon) | Verdict: not meaningfully affected by cloud latency — 20-500ms is 0.001-0.01% of a one-minute holding period. | strategy | crypto-latency-cloud-vs-colo.md — section 4 | CAPTURED |
| DEX consensus-latency awareness (dYdX/Hyperliquid) | Recognizes that block time/consensus (dYdX ~1-1.2s, Hyperliquid median 0.2s/p99 0.9s), not network RTT, dominates order latency on app-chain DEXs — region choice yields only tens-to-~200ms improvement. | market-data | crypto-latency-cloud-vs-colo.md — section 3-4 | CAPTURED |
| Funding-rate mechanics tracking across venues | Tracks each venue's funding formula and settlement cadence (Binance/Bybit/OKX 8h clamp±0.05%; dYdX v4 hourly with 0% default interest; Hyperliquid hourly, uncapped-by-comparison, paid on oracle price). | market-data | crypto-native-market-structure.md — section 1, Funding rate mechanics | CAPTURED |
| Funding-rate arbitrage (cash-and-carry) | Buy spot + short perp equal notional for delta-neutral funding collection when perp trades at premium; not latency-sensitive by construction (rebalances on funding-interval/hourly timescale). | strategy | crypto-native-market-structure.md — section 2, Funding-rate arbitrage / crypto-latency-cloud-vs-colo.md section 4 | CAPTURED |
| Funding-flip risk monitoring | Monitors for regime shifts (spot ETF flows, macro shock) that can flip funding negative for extended periods, turning a funding collector into a payer. | risk | crypto-native-market-structure.md — section 2 | CAPTURED |
| Margin/liquidation risk management on short-perp leg | Manages the leveraged short-perp leg of a funding-arb trade so a sharp spot rally doesn't force liquidation before the spot gain offsets it, especially under isolated margin with tight buffers. | risk | crypto-native-market-structure.md — section 2 | CAPTURED |
| Dual-venue custody-risk awareness in funding arb | Recognizes that spot and perp legs of a cash-and-carry trade sitting on different venues means an exchange insolvency wipes out the position asymmetrically (only one leg exposed). | risk | crypto-native-market-structure.md — section 2 | CAPTURED |
| Spot-perp basis vs dated-futures basis distinction | Distinguishes perpetual funding-collection ("basis" colloquially) from true dated-futures basis trading (CME BTC/ETH, spot-ETF-vs-CME-future), noting crypto basis is driven by retail leverage demand/thin liquidity rather than cost-of-carry. | market-data | crypto-native-market-structure.md — section 3 | CAPTURED |
| Liquidation-cascade mechanics awareness | Models forced liquidations as price-insensitive market orders whose impact triggers further liquidations in a positive feedback loop; documented in the May 2021 and Oct 2025 cascades ($8B and $19B liquidated respectively). | risk | crypto-native-market-structure.md — section 4, Liquidation cascades | CAPTURED |
| Insurance fund / queue-based ADL awareness | Tracks each CEX's insurance-fund and auto-deleveraging design (queue ranked by PnL% × leverage) as the backstop mechanism when liquidation execution falls short of bankruptcy price. | risk | crypto-native-market-structure.md — section 5, Insurance funds and ADL | CAPTURED |
| ADL over-correction risk | Documents a real failure mode where queue-based ADL over-corrected, expending 8x the actual deficit in haircuts during the Oct 10 2025 Hyperliquid event ($704.6M vs $304.5M deficit). | risk | crypto-native-market-structure.md — section 5 | CAPTURED |
| Cross vs isolated margin mode selection | Weighs cross margin (pools account equity, single bad bet drains buffer for all positions) against isolated margin (caps blast radius per strategy at cost of capital efficiency) as a risk-control tradeoff. | risk | crypto-native-market-structure.md — section 6, Cross vs isolated margin | CAPTURED |
| Portfolio margin mode | Notes OKX/Bybit also offer a third, net-risk-based portfolio margin mode distinct from cross/isolated. | risk | crypto-native-market-structure.md — section 6 | CAPTURED |
| Oracle price / mark price construction (DEX) | Tracks how DEXs (Hyperliquid) construct oracle price (weighted median of CEX spot mids) and mark price (blended EMA formula) for margining/liquidation, designed to resist single-venue manipulation. | market-data | crypto-native-market-structure.md — section 7, Oracle-based liquidation on DEXs | CAPTURED |
| Oracle-manipulation risk on thin-liquidity assets | Flags the risk that an attacker can pump a thin-liquidity asset's spot price across the same exchanges feeding the oracle to manipulate DEX mark price and threaten the backstop vault (documented JELLY incident, March 2025, resolved only by manual validator override). | risk | crypto-native-market-structure.md — section 7 | CAPTURED |
| Validator-level MEV/front-running risk (dYdX) | Recognizes that dYdX v4 validators control orderbook matching and can theoretically reorder/front-run trades before consensus commit; currently monitored via a public dashboard/MEV committee rather than eliminated by design. | risk | crypto-native-market-structure.md — section 8, MEV and sandwich risk | CAPTURED |
| Latency-based order-racing risk (Hyperliquid) | Notes that classic AMM sandwiching doesn't apply on Hyperliquid's CLOB, but sophisticated searchers can front-run visible large taker orders via faster placing/canceling on the fully public order book. | risk | crypto-native-market-structure.md — section 8 | CAPTURED |
| Post-only/ALO order type to mitigate DEX MEV-adjacent risk | Uses post-only/ALO order types as the practical mitigation for order-book-racing risk on dYdX/Hyperliquid, since there is no public mempool for Flashbots-style protection. | execution | crypto-native-market-structure.md — section 8, Practical implication | CAPTURED |
| Order-flow imbalance (OFI) signal, multi-level/robustified | Uses net change in bid/ask queue size (limit, market, cancel events) to predict short-term price moves; strongest crypto-specific evidence of the four signals reviewed, but only in depth-weighted/robust form — naive level-1 OBI is unsuitable (collapses to noise during volatility breakouts) and the signal decays within tens of seconds. | feature-engineering | crypto-orderbook-signals.md — section 1, Order-Book/Order-Flow Imbalance | CAPTURED |
| Spoofing-aware order-book-imbalance handling | Accounts for empirically documented manipulation of OBI — an estimated 31% of large orders in one sample could profitably spoof the imbalance signal by placing orders away from best price to avoid detection. | risk | crypto-orderbook-signals.md — section 1 | CAPTURED |
| Microprice fair-value estimator (Stoikov) | A short-term fair-value estimator beating mid-price/weighted-mid; crypto-specific validation is thin (one solid SSRN paper), and the original discretized formulation faces crypto-specific tick-size heterogeneity concerns. | feature-engineering | crypto-orderbook-signals.md — section 2, Microprice | CAPTURED |
| Volume-Adjusted Mid-Price (VAMP) | A microprice-family variant found in one crypto-specific study (Bitstamp BTC LOB) to outperform both trade- and quote-imbalance-adjusted microprice for short-term direction and large moves. | feature-engineering | crypto-orderbook-signals.md — section 2 | CAPTURED |
| Kyle's lambda (liquidity/impact descriptor) | Estimable in crypto and responds to crypto-native drivers (e.g. blockchain settlement latency), but multiple studies find it economically weak/redundant as a standalone alpha signal once OFI, Roll measure, or VPIN are included — useful for liquidity-regime description/sizing, not as an alpha signal. | feature-engineering | crypto-orderbook-signals.md — section 3, Kyle's Lambda | CAPTURED |
| VPIN (flow-toxicity metric) | Measures order-flow toxicity to predict volatility/jumps; has genuine crypto-specific peer-reviewed support (predicts Bitcoin price jumps) but sits on top of an unresolved equity-market critique (Andersen & Bondarenko) that the crypto literature hasn't engaged with — the most contested of the four signals reviewed. | feature-engineering | crypto-orderbook-signals.md — section 4, VPIN | CAPTURED |
| Binance markPrice/forceOrder WebSocket streams | markPrice@1s, !markPrice@arr@1s, forceOrder, and !forceOrder@arr streams on Binance USDⓈ-M accept subscription and are listed active but deliver zero frames from this host (verdict: withheld/unavailable server-side), while trade/depth/aggTrade streams on the same connection flow normally. | market-data | binance-withheld-streams.md — The finding | CAPTURED |
| REST polling fallback for mark price/funding | Recovers mark price, index price, settlement price, and funding rate by polling /fapi/v1/premiumIndex at 1Hz per symbol (weight 1/symbol) instead of relying on the withheld WS stream; stored under a distinct premiumIndex stream name rather than mislabeled as markPrice. | market-data | binance-withheld-streams.md — What was done about it | CAPTURED |
| WS subscribe-acknowledgment validation trap | Documents that Binance's WS endpoint accepts and lists even fabricated/invalid stream names as "active," so a subscribe acknowledgment is not evidence of a working feed — only measured frame counts are valid evidence. | observability | binance-withheld-streams.md — The trap that cost the most time | CAPTURED |
| Idle canary subscription for feed-availability monitoring | Keeps the silent forceOrder subscription active (at zero cost) specifically so the system will notice if Binance ever starts delivering liquidation data, since there is currently no REST replacement (allForceOrders was withdrawn from the public API). | observability | binance-withheld-streams.md — What was done about it | CAPTURED |
| Liquidation-feed unavailability rendered as red/unmeasured | Requires that the liquidation-data tile/status genuinely render as unavailable/red rather than default to a false-healthy state, since the feed does not currently exist for this system. | observability | binance-withheld-streams.md — What was done about it | CAPTURED |
| Fractional (Half) Kelly position sizing | Bets a fraction of the Kelly-optimal size; growth scales as f(2-f) so half-Kelly gives 75% of growth at 25% of variance. Recommended as a shrunk sanity CEILING on top of vol targeting, never a direct target, because Kelly is brutally sensitive to error in the mean-return estimate. | risk | allocation-and-regime.md — "Position sizing — the counterintuitive result" | CAPTURED |
| Volatility targeting position sizing | Sizes positions so forecast portfolio volatility hits a fixed target (e.g. 10% annualized); needs only a variance forecast, sidestepping the hard-to-estimate mean. Stated as the dominant real-world sizing approach and recommended as the primary sizer. | risk | allocation-and-regime.md — "Position sizing — the counterintuitive result" | CAPTURED |
| Empirical "starving" allocator | Capital-reallocation approach across strategies already decided in DECISIONS.md; judged more robust than any formal optimizer at 3–5 strategy scale because it doesn't require trusting a covariance estimate built on thin data. | portfolio | allocation-and-regime.md — "Allocation across strategies" | CAPTURED |
| Risk parity / equal risk contribution allocation | Allocates so each strategy contributes equal risk; needs only a covariance matrix, no expected-return estimate. Verdict: "the right default" at 3–5 strategies. | portfolio | allocation-and-regime.md — "Allocation across strategies" | CAPTURED |
| Hierarchical Risk Parity (HRP) | Clustering-based allocation (López de Prado 2016) robust at large N. Verdict: defensible but not clearly better at 3–5 branches; skip the clustering/linkage tuning; contested — clustering step called unstable under small correlation-matrix perturbations (unverified critique). | portfolio | allocation-and-regime.md — "Allocation across strategies" | CAPTURED |
| Mean-variance optimization (MVO) | Classic Markowitz optimizer. Verdict: an "error-maximization" procedure (Michaud 1989) that overweights whichever strategy got luckier with only 3–5 strategies and a year of data — "don't bother." | portfolio | allocation-and-regime.md — "Allocation across strategies" | CAPTURED |
| Black-Litterman allocation | Combines an equilibrium prior with investor views. Verdict: "don't bother" — its value is an equilibrium prior reverse-engineered from market caps, and there is no equilibrium to recover from 3 internal branches. | portfolio | allocation-and-regime.md — "Allocation across strategies" | CAPTURED |
| Drawdown-constrained optimization (CDaR) | Optimizes allocation against the drawdown path rather than variance. Verdict: don't build the full LP formulation, but keep the underlying concept. | portfolio | allocation-and-regime.md — "Allocation across strategies" | CAPTURED |
| Correlation-regime-split analysis | Computes strategy correlations split by volatility regime (worst 5% of market-vol days vs. rest) to reveal tail-only correlation spikes that unconditional correlation misses. Described as the cheapest high-value, most underused check. | risk | allocation-and-regime.md — "Correlation breakdown — mechanism, not bad luck" | CAPTURED |
| Tail-dependence-based position sizing | Sizes on tail-dependence measures rather than Pearson correlation, since crisis correlation is understated by matrices estimated over calm windows. | risk | allocation-and-regime.md — "Correlation breakdown — mechanism, not bad luck" | CAPTURED |
| Correlation-spike circuit breaker | Portfolio-level trigger: if realized cross-strategy correlation over a rolling 5–10 day window jumps above baseline, de-risk across the board because the diversification assumption has failed in real time. Distinct from strategy-level breakers; goal is faster detection, not prevention (accepted as partially irreducible). | risk | allocation-and-regime.md — "Correlation breakdown — mechanism, not bad luck" | CAPTURED |
| Qualitative liquidity/leverage overlap audit | Checks whether strategies share venue, collateral, or lending counterparty; if so the tail dependency lives in operational plumbing and backtest correlation numbers are irrelevant. | risk | allocation-and-regime.md — "Correlation breakdown — mechanism, not bad luck" | CAPTURED |
| HMM-based regime detection | Hidden Markov Model state labeling of market regimes. Verdict: "mostly hindsight-fit — do not give it veto power." State labels are assigned post-hoc by a human, detection is laggy by construction, and state count/priors are fit on history and overfit to the backtest window. | models | allocation-and-regime.md — "Regime detection — blunt assessment" | CAPTURED |
| Volatility regime detection (trailing realized vol threshold) | Measures trailing realized vol and thresholds it to scale exposure down in the high decile; no hidden-state inference needed. Called the exception with the best reliability-to-complexity ratio among regime methods, actionable without claiming to know why. | models | allocation-and-regime.md — "Regime detection — blunt assessment" | CAPTURED |
| CUSUM change-point detection (regime) | Sequential method that flags only that something changed, not what. Called the most defensible change-point method in this section for exactly that modesty of claim. | models | allocation-and-regime.md — "Regime detection — blunt assessment" | CAPTURED |
| BOCPD (Bayesian Online Change Point Detection) | Adams & MacKay (2007) method, more principled about uncertainty in *when* a change occurred than CUSUM. | models | allocation-and-regime.md — "Regime detection — blunt assessment" | CAPTURED |
| Regime signal as a weak feature into a meta-model | Design implication: do not add a standalone regime detector with decision authority; feed volatility regime as one weak feature into the meta-model over the experiment ledger (BULL/BEAR agents + arbiter + promotion gate), where a wrong regime read gets corrected by realized performance. | intelligence | allocation-and-regime.md — "Regime detection — blunt assessment" | CAPTURED |
| Mechanism-based strategy decay monitoring | Monitors the causal mechanism behind an edge (e.g. spread compression, opportunity frequency) directly rather than waiting for P&L significance. Called the highest-value practice, requiring knowing why each strategy makes money. | risk | allocation-and-regime.md — "Strategy decay — the hardest live judgment" | CAPTURED |
| CUSUM-style sequential Sharpe monitoring (decay) | Sequential monitoring of Sharpe built for "watch continuously, tell me on real evidence" without the false-positive inflation of daily repeated fixed-window t-tests (the peeking problem). | risk | allocation-and-regime.md — "Strategy decay — the hardest live judgment" | CAPTURED |
| Graduated probation ladder for strategy retirement | Throttles a decaying strategy in stages and requires sustained multi-window evidence before retiring, rather than a binary kill. A binary kill on first significance biases the surviving pool toward noise-lucky strategies. | risk | allocation-and-regime.md — "Strategy decay — the hardest live judgment" | CAPTURED |
| Survivable-by-design capital allocation | Small per-branch allocation and capped downside so no single wrong kill/keep decision hurts much, since perfect strategy-decay detection is unattainable (fundamental signal-to-noise limit). | risk | allocation-and-regime.md — "Strategy decay — the hardest live judgment" | CAPTURED |
| VaR (Value at Risk) | Single-threshold risk measure. Verdict: "false comfort" — says nothing about magnitude beyond the threshold and is not sub-additive (portfolio VaR can exceed the sum of its parts'). | risk | allocation-and-regime.md — "Risk measures" | CAPTURED |
| CVaR / Expected Shortfall | Coherent tail-risk measure (Rockafellar & Uryasev 2000), convex and optimizable by LP. Verdict: "if you act on one tail number, use this." | risk | allocation-and-regime.md — "Risk measures" | CAPTURED |
| EVT / GPD tail fitting | Extreme value theory / generalized Pareto tail fitting. Verdict: real and respected technique but wrong data regime here — needs many tail exceedances not available at months-to-years of history; revisit later. | risk | allocation-and-regime.md — "Risk measures" | CAPTURED |
| Bootstrap max-drawdown distribution | Block bootstrap (preserving autocorrelation) of the max-drawdown distribution instead of quoting one realized worst case. Called cheap, directly actionable, and underused relative to its value; makes circuit-breaker thresholds non-arbitrary. | risk | allocation-and-regime.md — "Risk measures" | CAPTURED |
| Simulated-drawdown-anchored circuit-breaker thresholds | Anchors limits (e.g. daily-loss limit) to a percentile of the simulated 1-day P&L distribution rather than round numbers/committee judgment, so thresholds are traceable to an inspectable assumption. | risk | allocation-and-regime.md — "Circuit breakers — making thresholds defensible" | CAPTURED |
| Graduated de-risking ladder (circuit breaker) | Staged response e.g. −5% trailing → cut gross 25%; −10% → cut 50%; −15% → flat, with each rung requiring short-window confirmation so a single bar can't trip it. | risk | allocation-and-regime.md — "Circuit breakers — making thresholds defensible" | CAPTURED |
| Kelly-fraction / circuit-breaker consistency check | Explicit check that sizing (which implies a tolerable drawdown) is consistent with the breaker ladder; sizing that implies 15% drawdowns as routine paired with a 10% kill switch is flagged as a design bug. | risk | allocation-and-regime.md — "Circuit breakers — making thresholds defensible" | CAPTURED |
| Circuit-breaker trigger logging | Logs every breaker trigger, true and false, in the same experiment ledger so breaker calibration is itself empirically tracked. | observability | allocation-and-regime.md — "Circuit breakers — making thresholds defensible" | CAPTURED |
| GP / symbolic-regression alpha discovery | Genetic-programming/symbolic-regression search over formulaic trading signals. Verdict: every primary source points the same direction — a multiple-comparisons problem at industrial scale; no audited, live, real-capital evidence found that it produces durable edge in equities or crypto. | strategy | alpha-discovery-gp-symbolic-regression.md — "Bottom line up front" | CAPTURED |
| WorldQuant "101 Formulaic Alphas" | Published, hand-selected-for-simplicity alpha set (Kakushadze 2016) — not GP-discovered despite common association. Reported Sharpes 1.24–4.16 but gross of transaction costs on high-turnover (0.6–6.4 day holding) signals; public since 2015-16 and likely crowded/decayed by now (inference, not directly re-tested). | strategy | alpha-discovery-gp-symbolic-regression.md — "1. WorldQuant '101 Formulaic Alphas'" | CAPTURED |
| gplearn (symbolic regression library) | General-purpose, scikit-learn-compatible symbolic regression tool; domain-agnostic, zero finance-specific validation in its own documentation. | models | alpha-discovery-gp-symbolic-regression.md — "2. gplearn and GP-for-alpha GitHub tools" | CAPTURED |
| AlphaGen (RL + GP alpha search framework) | Primarily an RL (PPO) framework that also ships GP and Deep Symbolic Optimization baselines for alpha search; trading-integration code marked "Experimental," no disclosed transaction costs or live-trading claim. | models | alpha-discovery-gp-symbolic-regression.md — "2. gplearn and GP-for-alpha GitHub tools" | CAPTURED |
| PySR (symbolic regression tool) | General symbolic regression software with a peer-reviewed software review, but no peer-reviewed PySR-in-finance application found. | models | alpha-discovery-gp-symbolic-regression.md — "3. Symbolic regression more broadly (PySR, DSO)" | CAPTURED |
| DSO (Deep Symbolic Optimization) | Won 1st place in the Real-World Track of the 2022 SRBench competition, entirely in physics/scientific-discovery domains; no DSO-in-finance peer-reviewed work found. | models | alpha-discovery-gp-symbolic-regression.md — "3. Symbolic regression more broadly (PySR, DSO)" | CAPTURED |
| Staged validation gate for GP-discovered alphas | Recommended practice if GP/symbolic-regression alpha search is pursued at all: rigorous deflated-Sharpe/PBO-style validation (not a single train/test split), realistic crypto-cost modeling, and mandatory live paper-trading before any capital allocation. | validation | alpha-discovery-gp-symbolic-regression.md — "5. Overall skeptical assessment" | CAPTURED |
| Published-factor/anomaly decay after publication | Published anomalies show ~26% lower OOS returns pre-publication (data-mining/selection bias) and ~58% lower post-publication (McLean & Pontiff 2016); ~65% of ~450 published anomalies fail to replicate (Hou/Xue/Zhang 2020). Implication: public formulaic alphas should be assumed to have little residual edge. | strategy | alpha-discovery-gp-symbolic-regression.md — "4. Realistic hit rates" / validation-beyond-deflated-sharpe.md — "What is worth implementing" | CAPTURED |
| Catastrophic forgetting (failure mode) | Neural nets store knowledge in distributed overlapping weights; unconstrained gradient updates on new data overwrite old-behavior weight configurations. Naive online fine-tuning risks silently destroying a model's handling of a regime unseen for months, undetected until it recurs. | models | continual-learning-and-failure-modes.md — "A.1 Catastrophic forgetting" | CAPTURED |
| EWC (Elastic Weight Consolidation) | Kirkpatrick et al. 2017 — quadratic penalty (weighted by Fisher information) discouraging changes to weights important for prior tasks. Verdict: validated mostly on vision/RL benchmarks with clean task boundaries; a published study (Ehret et al. 2021) shows EWC specifically struggles on RNNs — the natural architecture for sequential market data — because high working-memory requirements saturate Fisher-based importance estimates, making the network rigid rather than plastic. | models | continual-learning-and-failure-modes.md — "A.2 Mitigations — honest domain-gap assessment" | CAPTURED |
| Experience replay / rehearsal | Continual-learning mitigation that replays past data alongside new data to resist forgetting. | models | continual-learning-and-failure-modes.md — "A.2 Mitigations — honest domain-gap assessment" | CAPTURED |
| Synaptic Intelligence | Zenke et al. 2017 continual-learning mitigation, weight-importance-based like EWC. | models | continual-learning-and-failure-modes.md — "A.2 Mitigations — honest domain-gap assessment" | CAPTURED |
| Gradient Episodic Memory (GEM) | Lopez-Paz & Ranzato 2017 continual-learning mitigation constraining gradient updates using episodic memory of past tasks. | models | continual-learning-and-failure-modes.md — "A.2 Mitigations — honest domain-gap assessment" | CAPTURED |
| Progressive networks | Continual-learning architecture that adds new network capacity per task rather than overwriting shared weights. | models | continual-learning-and-failure-modes.md — "A.2 Mitigations — honest domain-gap assessment" | CAPTURED |
| Knowledge distillation (continual learning) | Transfers knowledge from a prior model into a new one as a forgetting-mitigation technique. | models | continual-learning-and-failure-modes.md — "A.2 Mitigations — honest domain-gap assessment" | CAPTURED |
| CLA (finance-specific continual-learning memory) | Dynamic-time-warping-based recall method (Philps et al. 2019) tested on real EM equity selection; beat generic CL approaches, while standard LSTMs underperformed even a simple CL-augmented feedforward model. Authors themselves note it's unclear how well generic CL transfers to noisy, non-stationary financial time series. | models | continual-learning-and-failure-modes.md — "A.2 Mitigations — honest domain-gap assessment" | CAPTURED |
| Plasticity Driven Knowledge Transfer | Katsikas, Passalis & Tefas (ICPR 2024) — experience replay + teacher-ensemble transfer for continual DRL in crypto trading; beats greedily-fine-tuned baselines, but backtest-only, no live validation reported. | models | continual-learning-and-failure-modes.md — "A.2 Mitigations — honest domain-gap assessment" | CAPTURED |
| ReCAP (Regime-Adaptive Continual Learning for Portfolio Management) | Pan et al. 2026 preprint — reports 164.89% cumulative return / 1.14 Sharpe vs. 124.24% / 0.92 for plain rolling-window retraining across 5 real datasets, 17 years; genuine but recent and narrow, no live/paper-trading validation reported. | models | continual-learning-and-failure-modes.md — "A.2 Mitigations — honest domain-gap assessment" | CAPTURED |
| Walk-forward optimization / rolling-window retraining | Trains on a rolling window, tests on the next OOS slice, rolls forward, repeats. Stated as the unambiguous practitioner workhorse — standard default per multiple platforms — over any exotic continual-learning ML. Caution elsewhere: often mistaken for sufficient protection against leakage, but still leaks when labels span multiple bars unless paired with purging. | validation | continual-learning-and-failure-modes.md — "A.3 What practitioners actually use" | CAPTURED |
| Regime-conditioned strategy switching | Maintains separate regime-conditioned strategy sleeves (most commonly via HMMs) and switches sleeves/cuts leverage/widens stops per detected regime, rather than continuously retraining one model — sidesteps catastrophic forgetting by construction. Second most common practitioner tool after walk-forward retraining. | models | continual-learning-and-failure-modes.md — "A.3 What practitioners actually use" | CAPTURED |
| Ensemble/strategy portfolio with performance-based reweighting | Allocates across multiple uncorrelated strategies and dynamically down-weights/pauses unhealthy ones rather than retraining any single model online. | portfolio | continual-learning-and-failure-modes.md — "A.3 What practitioners actually use" | CAPTURED |
| Shadow-Before-Swap (SBS) retrain-promotion gate | Retrains a challenger model on schedule but only promotes it over the live incumbent after a shadow trial on the next week's delayed labels shows a real, pre-registered advantage. Reported 78.4% fewer deployed-state changes with equal/better forecast quality vs. blind promote-on-retrain (crypto forecasting, 2026 preprint). | governance | continual-learning-and-failure-modes.md — "A.3 What practitioners actually use" | CAPTURED |
| Retrain-vs-retire decision framework | Maps observed signal patterns (gradual Sharpe decline with rising costs, step-down tied to a venue/rule change, live-vs-shadow divergence at size, repeated "fixing" that decays faster each time) to causes (crowding, microstructure change, capacity saturation, structural break) and prescribed responses (retire, retrain, reduce allocation). Core heuristic: one statistical signal triggers investigation only; two independent or one structural signal triggers retirement/size cut; thresholds should be pre-committed before a drawdown starts. | governance | continual-learning-and-failure-modes.md — "A.4 Retrain vs. retire criteria" | CAPTURED |
| Automated, verified, identical deployment across all nodes | Lesson from Knight Capital ($440-460M lost in 45 minutes, Aug 2012): a manual deployment missed 1 of 8 production servers, reactivating dormant legacy code under a reused flag with no second-engineer review. No single point of manual, unverified deployment should be able to put live capital at risk. | build-process | continual-learning-and-failure-modes.md — "B.1 Knight Capital, Aug 1 2012" | CAPTURED |
| Fast, tested kill switch | Non-negotiable safety control identified from the Knight Capital postmortem (SEC cited insufficient migration testing and no effective real-time monitoring/kill switch) alongside verified rollout. | risk | continual-learning-and-failure-modes.md — "B.1 Knight Capital, Aug 1 2012" | CAPTURED |
| Dead-code / flag-reuse hygiene | Guards against a retired strategy's execution path, parameter set, or config flag being silently reactivated by a promotion/retirement state-machine bug, without the safety checks the current strategy generation has — the direct analogue of Knight Capital's reused flag reactivating dormant Power Peg code. | build-process | continual-learning-and-failure-modes.md — "B.1 Knight Capital, Aug 1 2012" | CAPTURED |
| Second-engineer code review / change-management discipline | SEC action cited code review and change-management failures, no algorithm inventory, and insufficient migration testing as root causes of the Knight Capital incident. | build-process | continual-learning-and-failure-modes.md — "B.1 Knight Capital, Aug 1 2012" | CAPTURED |
| Newly-promoted-strategy hard circuit breakers | Lesson from the May 6 2010 Flash Crash (a fleet of individually "correct" algorithms produced systemic catastrophe with no single buggy component): a newly-promoted strategy needs its own hard circuit breakers — max participation rate, price-band sanity checks, automatic pause on abnormal volatility — because the risk is interaction with the rest of the strategy fleet and market-wide liquidity under stress, not just standalone behavior. | risk | continual-learning-and-failure-modes.md — "B.2 May 6 2010 Flash Crash" | CAPTURED |
| Over-trading / fee-burn guardrail | Guards against excessive trade frequency eating capital in fees, as observed in the Alpha Arena benchmark (Gemini 2.5 Pro made 238 trades, losing 13%+ of capital to fees; ended -56.71%). | risk | continual-learning-and-failure-modes.md — "B.3 ML-driven/'self-improving' trading system failures" | CAPTURED |
| Mandatory hedging/stop-loss discipline | Guards against rigid one-directional exposure with no hedging or stop-loss discipline, as observed in Alpha Arena (Grok 4: 100% long throughout, no stop-loss, ended -45.3%). | risk | continual-learning-and-failure-modes.md — "B.3 ML-driven/'self-improving' trading system failures" | CAPTURED |
| Decision-conflict resolution under contradictory signals | Guards against decision paralysis when signals conflict, as observed in Alpha Arena (GPT-5 ended -62.66%, attributed partly to paralysis under conflicting signals). | intelligence | continual-learning-and-failure-modes.md — "B.3 ML-driven/'self-improving' trading system failures" | CAPTURED |
| Order-entry sanity checks / fat-finger guardrail | Guards against manual or automated entry errors like a decimal-point mis-place, illustrated by Alameda's Oct 2021 fat-finger flash crash (tens of millions lost) after explicitly deprioritizing risk-check infrastructure for speed. | execution | continual-learning-and-failure-modes.md — "B.3 ML-driven/'self-improving' trading system failures" | CAPTURED |
| Automated ledger reconciliation | Guards against a software bug in deposit/liability-tracking code silently compounding for months, illustrated by FTX/Alameda's accounting-ledger bug that understated liabilities from ~$500M to $8B over ~6 months before detection. | observability | continual-learning-and-failure-modes.md — "B.3 ML-driven/'self-improving' trading system failures" | CAPTURED |
| State/unit-handling validation for autonomous agent actions | Guards against unit/decimal confusion and loss of conversational state, illustrated by the Lobstar Wilde incident (Feb 2026): an autonomous agent misread "4 SOL" and transferred ~52.4 million tokens (~$250K-$441K) to a stranger. | operations | continual-learning-and-failure-modes.md — "B.3 ML-driven/'self-improving' trading system failures" | CAPTURED |
| Train/deploy distribution-shift bias detection | Guards against a model learning a structural bias from its training window (e.g. short bias learned from a 2014-2023 range-bound period) that fails when deployed into a different regime (2024-2026 bull run) — accuracy ≠ profitability, and label-distribution shift between train and deploy is the mechanism. Single-source, not independently verified. | models | continual-learning-and-failure-modes.md — "B.3 ML-driven/'self-improving' trading system failures" | CAPTURED |
| Evolutionary/DRL production-overfitting guard | Guards against the pattern reported in "The Red Queen's Trap" (arXiv:2512.15732, unconfirmed peer review): a DRL + genetic-evolution hybrid showed validation APY >300% vs. live capital decay >70%, attributed to overfitting of aleatoric uncertainty and survivor bias inherent to evolutionary selection under high variance. | validation | continual-learning-and-failure-modes.md — "B.3 ML-driven/'self-improving' trading system failures" | CAPTURED |
| Paper-trading harness integrity check | Guards against measurement bugs in the paper-trading harness itself; the Polymarket-autopsy case found three bug classes inflated paper PnL ~135×, and paper-EV was inversely predictive of live EV across 6 architectures (Spearman ρ=-0.43) — the best paper performers were the worst live performers. | validation | continual-learning-and-failure-modes.md — "B.3 ML-driven/'self-improving' trading system failures" | CAPTURED |
| Survivorship bias correction (crypto backtests) | Guards against inflated backtest returns from excluding delisted tokens; of ~24,000 tokens ever listed on CoinMarketCap, >58% are now dead, and a "buy top-20 altcoins" backtest example showed +2,800% with survivorship bias vs +680% without (>4x inflation). | validation | continual-learning-and-failure-modes.md — "B.4 Silent degradation" | CAPTURED |
| Decision-time leakage taxonomy | A rigorous benchmark (arXiv:2605.23959) isolates five leakage mechanisms in backtests (future-window-centered rolling features, full-sample-fit normalization, symmetric correlation graphs using future returns, same-day-close execution assumptions, using full-daily-bar info while assuming same-day-open fills); the two feature/execution-timing mechanisms dominate, inflating Sharpe by 20+ points. Key finding: chronological order alone does not define what information a model was allowed to use. | validation | continual-learning-and-failure-modes.md — "B.4 Silent degradation" | CAPTURED |
| Config-schema strict validation | Guards against silent field-dropping in config loading (e.g. a described case of `dataclass.from_dict()` silently defaulting leverage to 10x instead of 2x, producing a backtest signature that looked exactly like textbook overfitting when the real cause was a swallowed schema mismatch). Provenance of the specific case unverified; the failure mode itself is a standard, credible software-engineering risk. | operations | continual-learning-and-failure-modes.md — "B.4 Silent degradation" | CAPTURED |
| Backtested-Sharpe poor predictor caveat | An unverified secondary-summary finding: a study of 888 algorithmic strategies found backtested Sharpe ratios were poor predictors of real-world performance (R² < 0.025), and ~44% of published strategies failed to replicate backtested success — directionally consistent with the broader overfitting literature but the specific statistic is unverified. | validation | continual-learning-and-failure-modes.md — "B.5 Irreproducibility" | CAPTURED |
| Execution-friction / frictionless-fill modeling gap | Backtests commonly assume frictionless fills at mid-price and that stop-losses execute at guaranteed prices; real stop-losses become aggressive market orders during stress. A strategy can be statistically sound and still fail live purely from execution-cost modeling error — needs non-linear volume-dependent impact modeling and realistic fee/spread assumptions. | execution | continual-learning-and-failure-modes.md — "B.5 Irreproducibility" | CAPTURED |
| Deflated Sharpe Ratio (DSR) | Bailey & López de Prado (2014) — corrects observed Sharpe for selection bias by controlling for number of trials N, variance of Sharpes across trials, and non-normality (skew/kurtosis). Recommended as the live scoring function for every candidate, not a post-hoc bolt-on. | validation | multiple-comparisons-false-discovery-backtesting.md — "1. The core problem, stated precisely" | CAPTURED |
| Minimum Backtest Length (MinBTL) | Bailey & López de Prado closed-form bound: MinBTL ≲ 2·ln(N) / (expected Sharpe)². Example: with 5 years of data, trying more than ~45 independent configurations makes it near-certain to find an in-sample Sharpe-1.0 strategy whose true OOS Sharpe is zero. | validation | multiple-comparisons-false-discovery-backtesting.md — "1. The core problem, stated precisely" | CAPTURED |
| Probability of Backtest Overfitting (PBO) / CSCV | Bailey, Borwein, López de Prado & Zhu — combinatorially symmetric cross-validation estimates the probability the in-sample-best configuration underperforms the median configuration OOS. On pure noise, 8,800 configurations produced an in-sample Sharpe of 1.27 with 53% of OOS Sharpes negative — direct empirical evidence of the scale of the problem. | validation | multiple-comparisons-false-discovery-backtesting.md — "1. The core problem, stated precisely" | CAPTURED |
| White's Reality Check | White (2000) — bootstrap-based test of whether the *best* of many competing models has genuine predictive superiority over a benchmark; foundational data-snooping treatment predating the López de Prado program by 14 years. Verdict elsewhere: skip — Hansen's SPA strictly dominates it; running both is ceremony. | validation | multiple-comparisons-false-discovery-backtesting.md — "1. The core problem, stated precisely" / validation-beyond-deflated-sharpe.md — "What is worth implementing" | CAPTURED |
| Hansen's SPA (Superior Predictive Ability) test | Hansen (2005) — more powerful successor to White's RC, less distorted by padding the candidate pool with obviously-bad strategies. Recommended as the right promotion-gate test: "does the challenger beat the incumbent, corrected for variants tried." | validation | multiple-comparisons-false-discovery-backtesting.md — "1. The core problem, stated precisely" | CAPTURED |
| Harvey-Liu-Zhu t>3.0 multiple-testing hurdle | Harvey, Liu & Zhu (2016) — conventional t≈2.0 significance bar is invalid once cumulative multiple testing across a large literature/search is accounted for; propose t-stat > 3.0 as the adjusted bar for the equity "factor zoo," generalized here to any in-house search accumulating many backtests. | validation | multiple-comparisons-false-discovery-backtesting.md — "1. The core problem, stated precisely" | CAPTURED |
| Purged / Embargoed k-fold cross-validation | Removes training observations whose label window overlaps a test-fold observation's window (purging) and excludes a buffer window after each test fold (embargo), because standard k-fold assumes i.i.d. observations, which is false for overlapping, serially-correlated financial labels. | validation | multiple-comparisons-false-discovery-backtesting.md — "2. Purged/Embargoed k-fold CV and CPCV" | CAPTURED |
| Combinatorial Purged Cross-Validation (CPCV) | López de Prado (2018) — partitions data into N groups and enumerates combinatorial train/test group assignments (purged/embargoed each) to build a full empirical distribution of OOS Sharpe ratios rather than trusting one historical path; feeds DSR's variance term and PBO. Recommended for finalists only (screen with cheaper PBO first, ~8–12 groups) since combinatorics explode. | validation | multiple-comparisons-false-discovery-backtesting.md — "2. Purged/Embargoed k-fold CV and CPCV" | CAPTURED |
| Minimum Track Record Length (MinTRL) | Bailey & López de Prado's term for the minimum number of return observations needed before an observed Sharpe can be statistically distinguished from a benchmark at a chosen confidence level; gates how long a strategy must run before its Sharpe can be trusted, and should gate any capital-increase decision. | validation | multiple-comparisons-false-discovery-backtesting.md — "3. Practical structural controls" | CAPTURED |
| Bonferroni correction | Divides α by N to control family-wise error. Verdict: simple but nearly useless at automated-search scale — at N=10,000, α/N=0.000005 suppresses essentially everything including true positives. | validation | multiple-comparisons-false-discovery-backtesting.md — "3. Practical structural controls" | CAPTURED |
| Benjamini-Hochberg (BH/BHY) false discovery rate control | Controls the expected proportion of false positives among accepted discoveries; far more practical at scale than Bonferroni. Should be applied to a search system's full "promoted" list, not a single fixed t-stat cutoff. | validation | multiple-comparisons-false-discovery-backtesting.md — "3. Practical structural controls" | CAPTURED |
| Pre-registered untouched final holdout (statistical) | A slice of history/time period, or paper-trading itself, that the search process and human tuning never sees until one candidate is already selected, looked at exactly once — the practical embodiment of what RC/SPA statistically substitute for. | validation | multiple-comparisons-false-discovery-backtesting.md — "3. Practical structural controls" | CAPTURED |
| Staged OOS paper-trading before capital allocation | An additional holdout layer beyond cross-validation, since backtests still inherit look-ahead bias, survivorship bias, and execution-cost mis-modeling that no amount of CV fully removes; only real-time forward performance is immune to those specific biases (though it introduces its own noise, gated by MinTRL logic). | validation | multiple-comparisons-false-discovery-backtesting.md — "3. Practical structural controls" | CAPTURED |
| Population-Based Training (PBT) | Jaderberg et al. (2017, DeepMind) — trains N models in parallel, periodically kills the worst and copies weights+hyperparameters from a top performer with perturbation, discovering a hyperparameter schedule over training time. Verdict: conditional yes, only via Ray Tune's built-in scheduler, and only if genuinely training expensive neural nets with schedule-sensitive hyperparameters; skip if backtests are CPU-cheap and fast. | models | search-infrastructure-pbt-nas-bayesopt-bandits.md — "1. Population-Based Training (PBT)" | CAPTURED |
| Neural Architecture Search (NAS) | RL-controller or gradient-based (DARTS) search over network topology for a fixed supervised task. Verdict: "don't bother, clearest resume-driven-engineering candidate on the list" — wrong problem (trading-strategy discovery is a search over strategy logic, closer to GP than NAS) and institutional-lab-scale compute cost (original NAS ≈22,400 GPU-hours). | models | search-infrastructure-pbt-nas-bayesopt-bandits.md — "2. Neural Architecture Search (NAS)" | CAPTURED |
| Bayesian Optimization (Optuna-based hyperparameter search) | Probabilistic-surrogate + acquisition-function search (Snoek, Larochelle, Adams 2012) for sample-efficient tuning when each evaluation is costly. Verdict: build the minimal version — Optuna wrapped around an existing walk-forward backtest, capped ~50-100 trials, paired with strict validation; caveat that in the low-dimensional, low-trial regime of a small team it barely beats random search. | models | search-infrastructure-pbt-nas-bayesopt-bandits.md — "3. Bayesian Optimization" | CAPTURED |
| Multi-Armed Bandits / Thompson Sampling (live capital allocation) | Posterior-sampling allocation across live strategies (each strategy = an arm, reward = risk-adjusted return). Verdict: "build it — best-fitting technique for the stated problem, and cheap." Minimum viable version: discounted/sliding-window Thompson sampling over weekly risk-adjusted reward, with hard allocation caps and a manual override; needs non-stationary/delayed-feedback handling since vanilla bandit theory assumes i.i.d. prompt rewards. | portfolio | search-infrastructure-pbt-nas-bayesopt-bandits.md — "4. Multi-Armed Bandits" | CAPTURED |
| EXP3 (adversarial bandit algorithm) | Auer, Cesa-Bianchi, Freund, Schapire — makes no i.i.d. reward assumption, designed for adversarial/non-stationary rewards. Verdict: attractive in theory for regime-shifting markets but its guarantees are worst-case and it converges slower/explores more than necessary in markets that are noisy but not truly adversarial; practitioners lean toward discounted/sliding-window Thompson sampling instead. | portfolio | search-infrastructure-pbt-nas-bayesopt-bandits.md — "4. Multi-Armed Bandits" | CAPTURED |
| Experiment ledger (log every trial, including abandoned ones) | The prerequisite for every statistical control in this domain: if it does not log every trial, including failed/abandoned informal ones, PBO, DSR, and multiple-testing correction all compute over the wrong N. Stated as the single biggest failure mode: "we tried fifty variants before the one we wrote down." | governance | validation-beyond-deflated-sharpe.md — "The finding that reframes everything else" | CAPTURED |
| Triple-barrier labeling | AFML (ch. 3) labeling scheme: upper/lower/vertical barriers sized to local volatility; label is whichever is touched first, reflecting how a position would actually be risk-managed. Caveat: barrier width is itself a hyperparameter, and searching it is itself overfitting that must go through the same validation machinery. | feature-engineering | validation-beyond-deflated-sharpe.md — "Labeling and sampling" | CAPTURED |
| Meta-labeling | Secondary model predicting whether to act on a primary model's signal. Verdict: real value is bet sizing and precision, not an overfitting cure — this is true only against a fixed, already-decided primary model; searching over primary variants or a large meta feature set just moves the multiple-testing problem into a second layer. | models | validation-beyond-deflated-sharpe.md — "Three false-confidence traps" / "Labeling and sampling" | CAPTURED |
| Sample uniqueness / concurrency + sequential bootstrap | AFML (ch. 4) technique: overlapping labels violate IID, inflating effective sample size and understating variance. Called "the least contested part of the AFML toolkit," but sequential bootstrap is inherently non-parallelizable — a real engineering expense in an automated system. | validation | validation-beyond-deflated-sharpe.md — "Labeling and sampling" | CAPTURED |
| mlfinlab licensing-risk avoidance | `mlfinlab` is no longer open source (moved to a commercial/subscription model with a custom non-OSS license); recommendation is to not put a licensing risk in the capital-at-risk path — reimplement triple-barrier and sequential bootstrap directly, or use the community rewrite `mlfinpy` after checking its terms. | build-process | validation-beyond-deflated-sharpe.md — "Labeling and sampling" | CAPTURED |
| Bitemporal storage (event time + knowledge time) | Every datum carries both event time (when it happened) and knowledge time (when the system could have known it); all backtest queries filter on knowledge_time ≤ simulation_clock. Called the highest-leverage single fix for point-in-time correctness — a schema decision, not a discipline. | market-data | validation-beyond-deflated-sharpe.md — "Point-in-time correctness — architecture, not discipline" | CAPTURED |
| Append-only ingestion | Never overwrite a republished/corrected candle; store the correction as a new row. Overwriting makes "what did we know then" structurally unanswerable afterward. | market-data | validation-beyond-deflated-sharpe.md — "Point-in-time correctness — architecture, not discipline" | CAPTURED |
| Frozen universe snapshots | Snapshots which pairs were tradeable at each decision date; deriving the universe from a current mutable table instead reflects today's knowledge of who was ever listed, introducing survivorship bias. | market-data | validation-beyond-deflated-sharpe.md — "Point-in-time correctness — architecture, not discipline" | CAPTURED |
| Shared clock-gated data-access layer (backtest and live) | One data-access layer used by both backtest and live, clock-gated so lookahead is structurally impossible; if backtesting uses a separate, more permissive path, the two diverge and an off-by-one windowing bug produces a great backtest and a broken live system. | market-data | validation-beyond-deflated-sharpe.md — "Point-in-time correctness — architecture, not discipline" | CAPTURED |
| Wash-trading / fake-volume discount | Guards against using aggregate volume as a liquidity proxy for sizing; Bitwise's 2019 SEC submission found ~95% of reported BTC spot volume was fake/non-economic across 81 exchanges, with real volume concentrated on ~10. | market-data | validation-beyond-deflated-sharpe.md — "Crypto-specific data traps" | CAPTURED |
| Outage-correlated execution-availability haircut | Models exchange execution availability as a haircut correlated with realized volatility rather than a flat slippage constant, since exchanges throttle or fail exactly when a strategy most wants to trade. | execution | validation-beyond-deflated-sharpe.md — "Crypto-specific data traps" | CAPTURED |
| Backfilled/synthetic candle provenance tracking | Tracks per-candle provenance at ingestion to catch vendor-interpolated gaps, which smooth away the flash crashes that matter most for tail sizing. | market-data | validation-beyond-deflated-sharpe.md — "Crypto-specific data traps" | CAPTURED |
| Timestamp-convention risk handling | Accounts for open-vs-close marking, inclusive/exclusive bounds, and funding-rate settlement times differing per exchange — a frequent source of off-by-one look-ahead for anything trading around funding. | market-data | validation-beyond-deflated-sharpe.md — "Crypto-specific data traps" | CAPTURED |
| Delisted/rebranded pair data hygiene | Handles survivorship bias from delisted pairs and silent splits/merges from rebrands in historical data. | market-data | validation-beyond-deflated-sharpe.md — "Crypto-specific data traps" | CAPTURED |
| Paper trading (pre-live check) | Catches gross implementation bugs and gives a first read on latency. Explicitly does NOT catch overfitting — an overfit strategy just looks good for a while; its value is purely operational. | validation | validation-beyond-deflated-sharpe.md — "The backtest-to-live gap" | CAPTURED |
| Shadow deployment | Runs live logic against real order books, computing hypothetical fills with no orders placed — the best available check on the execution gap. No better than paper trading at catching overfitting. | validation | validation-beyond-deflated-sharpe.md — "The backtest-to-live gap" | CAPTURED |
| Tooling-enforced holdout pre-registration | The actual defense against overfitting, only if enforced by tooling: the ledger records a commit hash and a "holdout not yet consumed" flag, and CI blocks any run querying the holdout range before the strategy is frozen. | governance | validation-beyond-deflated-sharpe.md — "The backtest-to-live gap" | CAPTURED |
| Per-frequency-band purge/embargo configuration | Purge/embargo windows must be configured per frequency band since label horizons differ by orders of magnitude across trading brains; an embargo sized for an hours→minutes brain is wildly oversized for sub-second and undersized in reverse — one shared config across brains is flagged as wrong. | validation | validation-beyond-deflated-sharpe.md — "Purging and embargo — mechanics" | CAPTURED |
| Backtest-to-live gap contribution ranking | Ranked (agent's judgment, not cited) ordering of what most explains the backtest-to-live performance gap: overfitting not caught by the validation battery → execution reality (slippage, partial fills, latency, outages) → regime shift → point-in-time leakage → data contamination. | validation | validation-beyond-deflated-sharpe.md — "The backtest-to-live gap" | CAPTURED |
| PIT (point-in-time) join leakage guard | Model must only see feature values whose `created_timestamp <= T`, not just `event_timestamp <= T`; Feast's own `filter_by_created_timestamp` leakage protection defaults to False, so the tool ships with the exact leakage bug it's meant to prevent unless explicitly enabled. | feature-engineering | trading-feature-stores-experiment-tracking.md — Question 1: How PIT joins mechanically prevent leakage | CAPTURED |
| Feast feature store | Full offline/online/registry/feature-server architecture for PIT-correct feature serving; verdict is don't bother at $100k-1M solo scale — solves training-serving skew across many consumers/teams, a problem a solo trader doesn't have, and isn't built for tick/orderbook time-series (materialize() OOMs, no streaming/CEP support). | feature-engineering | trading-feature-stores-experiment-tracking.md — Feast architecture / VERDICT: Don't bother with a feature store | CAPTURED |
| Hand-rolled PIT join module (merge_asof/join_asof wrapper) | Lightweight feature-store alternative: Parquet lake with `event_time`/`ingestion_time`/`availability_time` columns, joined via `pandas.merge_asof`/`polars.join_asof` with `direction="backward"` and a staleness tolerance; a few hundred lines, git-diffable, no daemons, ~80% of a feature store's benefit. | feature-engineering | trading-feature-stores-experiment-tracking.md — Question 1: concrete alternative | CAPTURED |
| MLflow experiment tracking / model registry | Self-hosted experiment tracking (params/metrics/artifacts) with model promotion via aliases + tags (e.g. `champion`) rather than the deprecated Stages API; recommended solo setup is SQLite backend, run on-demand not as a 24/7 daemon, moving to Postgres only once genuinely concurrent writers exist. | build-process | trading-feature-stores-experiment-tracking.md — Question 2: MLflow / VERDICT and recommendation | CAPTURED |
| DVC data-snapshot versioning | Ties a data snapshot to a git commit via content hash (distinct from MLflow's run-tracking); recommended to add later, once a raw-data pipeline has real risk of "did this backtest run against stale/different data," not from day one. | build-process | trading-feature-stores-experiment-tracking.md — Question 2: DVC | CAPTURED |
| Weights & Biases — skip verdict | Free tier's "corporate use is not allowed" clause is a real compliance ambiguity for someone trading real capital through an entity, and self-hosted licensing for individuals is unconfirmed; team-collaboration features solve a coordination problem a solo operator doesn't have — skip entirely. | build-process | trading-feature-stores-experiment-tracking.md — Question 2: Weights & Biases / VERDICT | CAPTURED |
| Parquet archival tick store | Compressed columnar on-disk format for archival tick data; verdict is use Parquet, and specifically don't use Feather/Arrow IPC for anything kept long-term (measured 9x larger on disk despite fastest open time). | market-data | trading-market-data-storage.md — 1. Storage Formats | CAPTURED |
| Avoid building a custom binary tick format | Custom delta-encoded binary formats can beat Parquet 2-10x on bytes/tick, but real projects (Shinoji Research, a NautilusTrader RFC) chose Parquet/TimescaleDB over a hand-built format because the engineering/maintenance cost wasn't worth it — the clearest "don't bother" finding in market-data storage. | market-data | trading-market-data-storage.md — 1. Storage Formats (Custom binary formats) | CAPTURED |
| Symbol-explicit Parquet partitioning | Partition tick data explicitly by symbol (and date); a real production bug (`cryptostore`) from omitting `symbol` from the partition key silently mixed multiple symbols' data into one file — always partition explicitly by symbol, target ≥128MB files, batch/compact given low daily volume. | market-data | trading-market-data-storage.md — 1. Storage Formats (Partitioning) | CAPTURED |
| ZSTD compression for archival store | ZSTD (level ~5-9) is the compression codec verdict for the archival tick store — fast enough for hot paths and compresses meaningfully better than Snappy; Gzip is too slow except for cold archives. | market-data | trading-market-data-storage.md — 1. Storage Formats (Compression codec) | CAPTURED |
| kdb+ evaluation | Commercial kdb Insights tier remains $70-77k/year; a new free KDB-X Community Edition (GA Nov 2025) exists and is worth a spike, but q's genuine APL-lineage learning curve means skip kdb+ for a first build — Parquet+DuckDB gets ~90% of the value with zero new language. | market-data | trading-market-data-storage.md — 1. Storage Formats (kdb+ accessibility) | CAPTURED |
| NautilusTrader ParquetDataCatalog | PyArrow + fsspec (local/S3/GCS/Azure) with a Rust/DataFusion streaming backend for core market-data types (OrderBookDelta, QuoteTick, TradeTick, Bar), shared between backtesting and live persistence; if adopting NautilusTrader as the engine, write directly to this rather than building a parallel Parquet layer. | market-data | trading-market-data-storage.md — 1. Storage Formats (NautilusTrader's data catalog) | CAPTURED |
| TSDB selection for ASOF joins (ClickHouse/QuestDB over TimescaleDB) | Native ASOF JOIN is the decisive differentiator for backtesting: ClickHouse and QuestDB support it natively, TimescaleDB does not (its LATERAL JOIN workaround showed a documented 10x regression on hypertables) — if/when a TSDB is added, choose ClickHouse or QuestDB, not TimescaleDB. | market-data | trading-market-data-storage.md — 2. Time-Series Databases | CAPTURED |
| Parquet + DuckDB/Polars as default store (no TSDB) | A dedicated TSDB is premature for backtesting/research at solo scale; start with embedded Parquet + DuckDB/Polars for all historical storage/backtesting, and add a TSDB only when live-ingestion correctness (dedup, late-packet handling, backfill overlap, idempotency) becomes a felt problem, not for query speed or storage volume. | market-data | trading-market-data-storage.md — 2. Time-Series Databases (Is a TSDB even necessary?) | CAPTURED |
| Storage lifecycle tiering (NVMe → S3 Standard → Glacier) | Storage cost is a rounding error at this scale (<$400/year on any tier); reasonable lifecycle is local NVMe for live/working data, S3 Standard for a few months of fast reprocessing access, then Glacier for the long tail — not worth engineering time to optimize further. | market-data | trading-market-data-storage.md — 3. Realistic Volume and Cost | CAPTURED |
| PSI drift metric | Coarse bulk-shift detector on binned/discretized features using 1994 credit-scorecard thresholds with no statistical grounding for trading; Evidently's own benchmark found PSI stays silent until drift exceeds ~10% at 100k observations — verdict: don't use as a primary safety net, green PSI ≠ fine. | observability | trading-monitoring-deployment-discipline.md — A1. Drift detection metrics (PSI) | CAPTURED |
| KL-divergence / Jensen-Shannon drift metrics | KL-divergence is asymmetric and blows up at zero-probability bins; production systems substitute Jensen-Shannon divergence (symmetrized KL), which behaves similarly to PSI per Evidently's benchmark. | observability | trading-monitoring-deployment-discipline.md — A1. Drift detection metrics (KL-divergence) | CAPTURED |
| KS-test drift metric | Nonparametric CDF-distance test; at >100,000 observations it flags even 0.5% drift as statistically significant, causing continuous false alarms on tick/trade-level crypto data — route to a low-priority dashboard, never wire it to a page. | observability | trading-monitoring-deployment-discipline.md — A1. Drift detection metrics (KS test) | CAPTURED |
| Wasserstein/EMD drift metric | Magnitude-aware continuous drift metric with no p-value hypersensitivity; Evidently AI defaults to it above 1,000 reference observations, though there is no vendor consensus (WhyLabs uses Hellinger distance instead). | observability | trading-monitoring-deployment-discipline.md — A1. Drift detection metrics (Wasserstein) | CAPTURED |
| Fixed-reference drift detection under non-stationarity — structural failure | A 2026 "Window Dilemma" paper formally proves no reference-vs-current window boundary is correct under continuous drift; a plain model with no drift detector sometimes outperformed a drift-aware one — a fixed-reference PSI/KS check against training-time distribution is structurally the wrong tool for non-stationary financial features, not a tuning problem. | observability | trading-monitoring-deployment-discipline.md — A1. Does non-stationarity break naive drift detection? | CAPTURED |
| Technical-divergence vs statistical-decay diagnostic | Four-quadrant framework distinguishing "technical pipeline divergence" (a bug) from "statistical performance decay" (correct code, decayed predictions from regime change/overfitting/alpha decay); treating decay as a bug wastes debugging time, treating bugs as decay causes unnecessary model changes. | observability | trading-monitoring-deployment-discipline.md — A1. (Stefan Jansen framing) | CAPTURED |
| Deflated Sharpe Ratio (DSR) + CPCV pre-live gate | The actual mechanism practitioners use to distinguish real regime change from an overfit backtest, applied before going live so subsequent live decay can be attributed; DSR-gated promotion is cheap and prevents shipping overfit backtests. | validation | trading-monitoring-deployment-discipline.md — A1. (López de Prado) / Overkill-vs-essential table | CAPTURED |
| ADWIN/Page-Hinkley prediction-error-stream monitor | Stream-native sequential change detectors (ADWIN, Page-Hinkley, DDM) applied to the prediction-error stream rather than input features, purpose-built for non-stationary/streaming data; Page-Hinkley was found most responsive in real time. Verdict: essential and cheap, off-the-shelf in `river`. | observability | trading-monitoring-deployment-discipline.md — A1. Finance-appropriate combination | CAPTURED |
| Tiered data-quality gate pattern | Four-tier pattern converging across TFX/Chronon/Deequ: Tier 0 schema presence, Tier 1 null-rate/range/dedup thresholds, Tier 2 distribution drift, Tier 3 semantic invariants (timestamp monotonicity, "feature timestamp ≤ label timestamp" leakage guard, non-negativity). | validation | trading-monitoring-deployment-discipline.md — A2. Data quality gates | CAPTURED |
| DB-level dedup + OHLC invariant checks | Unique constraint on `(exchange_id, symbol, timeframe, timestamp)` enforced at the database (not app code) so it survives concurrent/crashed fetchers; OHLC sanity checks (`high≥low`, open/close within `[low,high]`, `volume≥0`); gap detection distinguishing expected (maintenance) vs unexpected gaps; validate on write, not on read. | validation | trading-monitoring-deployment-discipline.md — A2. Data quality gates (crypto-specific pattern) | CAPTURED |
| Skip batch data-quality frameworks in the live path | Great Expectations, Deequ, Evidently, Pandera, Soda Core are all batch/DataFrame-oriented (GE maintainers confirmed no streaming support); none are built for gating a live trading decision in real time. Verdict: hand-rolled assertions outperform them here; use these frameworks only for offline backtest-data hygiene. | validation | trading-monitoring-deployment-discipline.md — A2. Data quality gates (Tool verdict) | CAPTURED |
| Cross-exchange MAD-based outlier rejection | Hash price+volume payloads to detect a feed reporting stale-but-unchanged data with a moving timestamp; drop a source after N consecutive stale responses; use median absolute deviation, excluding anything >3×MAD from the median, because MAD requires manipulating a majority of sources to move it — directly prevents Compound/Mango-class oracle failures. | risk | trading-monitoring-deployment-discipline.md — A2. Cross-exchange sanity checks | CAPTURED |
| Heartbeat/dead-man's-switch — insufficient alone | Heartbeat/ping-pong proves the socket is alive, not that data is fresh or correct; a frozen order book on a live socket is a documented, more dangerous failure than disconnection and won't trip ping/pong — must be paired with sequence-gap detection and REST reconciliation. | risk | trading-monitoring-deployment-discipline.md — A3. Staleness detection | CAPTURED |
| Sequence-gap + activity-adaptive staleness detection | Track last-seen sequence per symbol/channel (gap triggers resync); activity-adaptive staleness threshold `max(floor, k × rolling_avg_inter_tick_interval)` instead of a fixed timeout; dual-path check distinguishing a data-path-specific freeze from full connectivity loss; REST-reconciliation loop gated by K-consecutive-divergent-checks before forcing a resnapshot. | observability | trading-monitoring-deployment-discipline.md — A3. Concrete detection heuristics | CAPTURED |
| Staged deployment sequence (backtest → paper → shadow-live → reduced-size live → full live) | Realistic promotion sequence for swapping a live model; blue-green maps poorly onto trading (instant full-capital cutover is the flag-day risk to avoid) — use champion/challenger with a traffic-split ramp instead. | governance | trading-monitoring-deployment-discipline.md — B1. Concepts and trading equivalents | CAPTURED |
| Shadow-trading exit-criteria gate (RustyBT-style) | Concrete shadow-stage gate: 7 consecutive stable days, ≥95% signal alignment rate, ≥90% execution-quality match, halt after 3 misalignments — a real working reference shape for the shadow-live stage. | governance | trading-monitoring-deployment-discipline.md — B1. The realistic sequence | CAPTURED |
| Shadow Before Swap (SBS) promotion method | Clone the incumbent, warm-refit the challenger off the serving path, evaluate both on the same forward window, promote only above a paired statistical-advantage threshold; measured 78.4% fewer deployed-state changes vs. an immediate-promote baseline across 48 weeks of crypto forecasting. | governance | trading-monitoring-deployment-discipline.md — B1. The realistic sequence | CAPTURED |
| Regime-coverage-gated promotion dwell time | Gate advancement on whether the system has observed a real drawdown/chop period and a real high-volatility event at each stage, not on calendar time — a model that paper-traded 60 days entirely in a trending bull regime has been validated against nothing that matters. | governance | trading-monitoring-deployment-discipline.md — B1. Dwell times | CAPTURED |
| Sharpe-divergence live-vs-backtest go/no-go gate | Documented norm is a median 73% Sharpe deterioration from backtest to live (215 strategies studied), so a 40-60% haircut alone isn't a kill signal; use a DSR-adjusted expectation as a pre-registration gate — a live Sharpe going negative or diverging from that DSR expectation is the actual signal. | validation | trading-monitoring-deployment-discipline.md — B1. Go/no-go metrics worth building | CAPTURED |
| Slippage realized-vs-modeled monitoring | No rigorous academic threshold exists for a slippage kill signal; the concrete implementable pattern is RustyBT's `execution_quality_threshold` (90% fill-rate match) as a first-class shadow-stage metric. | execution | trading-monitoring-deployment-discipline.md — B1. Go/no-go metrics worth building | CAPTURED |
| Signal-correlation live/shadow-vs-backtest gate | RustyBT's `signal_tolerance_pct` (5%) + `max_misalignment_count` (halt after 3) + alignment-rate gate (≥95%, circuit-breaker below) — the strongest, most concretely implementable go/no-go gate found. | validation | trading-monitoring-deployment-discipline.md — B1. Go/no-go metrics worth building | CAPTURED |
| Bootstrap/Monte Carlo drawdown budget sizing | The backtest's max drawdown is a lower bound, not an expectation (bootstrap/permutation reshuffling typically shows a deeper tail); size the kill-switch DD budget against the p75-p90 percentile of a bootstrap DD distribution of your own backtest, not a flat multiplier of the historical max-DD. | risk | trading-monitoring-deployment-discipline.md — B1. Max drawdown, live vs backtest | CAPTURED |
| Parallel-run with automated divergence flagging for platform swaps | Man Group's $30B SAS→Python migration ran old and new systems in parallel for ~3 years with automated cross-checking of outputs, flagging large correlation divergence, plus formal Investment Committee sign-off gating each incremental swap — avoids a flag-day migration. | governance | trading-monitoring-deployment-discipline.md — B2. Avoiding a "flag day" model swap | CAPTURED |
| Champion/challenger traffic-split ramp with reserve rollback | Regulator-formalized banking practice: start challenger at 5-20% of live volume, sequential testing to stop early, ramp 10%→25%→50%→100%, keep the old champion in reserve 30-90 days for instant rollback. | governance | trading-monitoring-deployment-discipline.md — B2. Champion/challenger with traffic-split ramping | CAPTURED |
| Position-blending linear shrinkage for model handoff | `w = alpha × old_weights + (1 − alpha) × new_weights`, grounded in James-Stein shrinkage literature, for gradual capital reallocation from an old model to a new one, avoiding flag-day risk. | execution | trading-monitoring-deployment-discipline.md — B2. Concrete position-blending mechanism | CAPTURED |
| chrony over ntpd for VM clock sync | chrony measured ~83x more accurate than ntpd under intermittent polling and explicitly adapts to VM-specific clock instability (e.g. host CPU steal time); use chrony pointed at the cloud's internal time-sync endpoint (e.g. AWS `169.254.123.123`) rather than ntpd. | operations | trading-operational-failures.md — 1. Clock drift and NTP | CAPTURED |
| Tuned recvWindow + clock-offset alerting | Set exchange `recvWindow` to 5000-10000ms, not the 60000ms max, so clock drift throws an immediate loud order-rejection error (Binance `-1021`) instead of silently eating a huge buffer; alert on `chronyc tracking` offset >1s and on "Leap status: Not synchronised," before it ever produces a rejected order. | operations | trading-operational-failures.md — 1. Clock drift and NTP | CAPTURED |
| UTC-everywhere discipline | Use UTC in code, logs, stored data, and explicit `TZ=UTC` in cron/systemd definitions; DST/local-timezone seams (funding-window announcements, cron defaults, mixed-timezone log display) are recurring silent bug sources that need rechecking twice a year, not a one-time conversion. | operations | trading-operational-failures.md — 2. Timezone / DST bugs | CAPTURED |
| Exchange API-change defensive patterns | Strict schema validation on every response (catches silent field/shape changes like Binance's permissions restructuring that a duck-typed parser would sail through); programmatic subscription to the exchange's changelog/announcement feed; scheduled contract tests against testnet (not just pre-deploy); pin and log the API/schema version coded against, alerting on a version-marker mismatch. | operations | trading-operational-failures.md — 3. Exchange API changes breaking things | CAPTURED |
| Order-book sequence-number (U/u) gap detection | Binance's documented diff-depth gap algorithm: if the event's first update ID (U) is greater than the local order book's update ID + 1, events were missed; every diff must chain U = last-processed u + 1, or resync via a fresh REST snapshot. | market-data | trading-operational-failures.md — 4. Silent data-feed gaps | CAPTURED |
| Silent-feed-gap detection beyond sequence numbers | Expected-message-rate alarms tracking time-since-last-message per symbol/channel independent of connection state; periodic REST-snapshot cross-check diffed against WS-derived local state (catches desync even when sequence chain and heartbeat both look fine); explicit testing of the reconnect/resubscribe path since a common bug silently drops one of N symbols' subscriptions on reconnect. | observability | trading-operational-failures.md — 4. Silent data-feed gaps (Detection mechanisms) | CAPTURED |
| Exponential backoff with full jitter on reconnect | AWS Full Jitter formula (`sleep = random(0, min(cap, base*2^attempt))`) as the recommended default for WS reconnects; on receiving an explicit 429/418, honor the `Retry-After` header directly rather than the own backoff schedule, since continuing to request during a ban re-extends it per Binance's own `-418` definition. | operations | trading-operational-failures.md — 6. Reconnection storms | CAPTURED |
| Shared reconnect semaphore + circuit breaker | Cap total concurrent reconnect attempts across all processes sharing an IP (a semaphore, since exchange bans are per-IP not per-key); add a circuit breaker that stops reconnecting and pages a human after N consecutive failures rather than backing off silently forever. | operations | trading-operational-failures.md — 6. Reconnection storms | CAPTURED |
| Write-ahead order-intent log with startup reconciliation | Persist order intents (client order ID, params, status `intent_created`→`sent`→`acknowledged`→`terminal`) to durable disk-synced storage before the request is sent; on every startup, reconcile the local log against the exchange's authoritative open-orders/positions/balances before resuming trading — starting the strategy loop before reconciliation finishes is how a restart turns into a duplicate order or an untracked position. | operations | trading-operational-failures.md — 7. State loss on restart / crash recovery | CAPTURED |
| Binary fill-state tracking bug | Code checking `if status == "FILLED"` and otherwise assuming "not filled, safe to retry full size" mishandles `PARTIALLY_FILLED` (e.g. 30% executed), doubling intended position size on retry; fix is to always track remaining quantity (`originalQty - executedQty`) as source of truth, never a binary flag. | execution | trading-operational-failures.md — 5. Partial fills mishandled | CAPTURED |
| Double-counting fills on reconnect after disconnect | If a partial fill lands and the connection drops before the final status arrives, retry logic that re-derives "how much is still needed" from local pre-disconnect state (instead of re-querying the exchange's authoritative status) can double-apply a fill or resend a fully-executed order; correct pattern is to always query the exchange's order-status endpoint directly on reconnect or doubt. | execution | trading-operational-failures.md — 5. Partial fills mishandled | CAPTURED |
| Position-size drift from rounding on many small partial fills | An execution algo (iceberg/TWAP) with an in-memory running position counter accumulates a tiny systematic rounding/precision error per fill versus the exchange's actual lot-size rules; individually invisible, it becomes a real discrepancy over thousands of fills — invisible until reconciliation catches it, or doesn't if reconciliation shares the same rounding assumption. | execution | trading-operational-failures.md — 5. Partial fills mishandled | CAPTURED |
| Client-order-ID fill-dedup mapping lost on restart | If fill events are deduplicated by client order ID and that mapping lives only in memory, a process restart between "partially filled" and "fully filled" can cause the restarted process to fail to recognize a late-arriving fill event, either double-processing or discarding it. | execution | trading-operational-failures.md — 5. Partial fills mishandled | CAPTURED |
| Disk-space exhaustion mitigation | `logrotate` with `copytruncate` (needed if the app holds the log file open for its whole lifetime); separate, shorter retention policy for tick/market data vs application logs (nightly compress-and-push to object storage); monitor both disk space AND inode usage (80%/90% alert thresholds, since many small per-symbol files can exhaust inodes while space looks fine); fail loud on `ENOSPC` rather than silently swallowing write errors, since a silently-failing write-ahead log defeats crash recovery exactly when disk pressure might also be causing instability. | operations | trading-operational-failures.md — 8. Disk space exhaustion | CAPTURED |
| Full RNG seeding checklist across the stack | Each randomness source (Python `random`, NumPy legacy seed vs. `Generator` API, `PYTHONHASHSEED`, PyTorch/CUDA, TensorFlow, NautilusTrader's engine-level seed, DataLoader worker RNG) needs independent seeding; seeding does not guarantee GPU floating-point non-associativity, cross-GPU-architecture, or cross-library-version reproducibility. | build-process | trading-reproducibility.md — 1. Seeding | CAPTURED |
| Snapshot-on-ingest immutable checksummed market data | Exchanges revise historical data after the fact (corrected/cancelled trades, recomputed OHLCV), so re-pulling "the same" date range later can silently return different numbers; snapshot on ingest, immutable, checksummed (Parquet + SHA256 manifest), and never re-pull historical data for backtesting — a real data correction is a new dataset version, not an overwrite. Flagged as the single highest-leverage reproducibility practice. | build-process | trading-reproducibility.md — 2. Data Versioning | CAPTURED |
| Dependency lockfiles (uv.lock/poetry.lock) | Pin the exact resolved version of every transitive dependency; a loose top-level pin (`numpy>=1.24`) with no lockfile resolves differently over time even with the requirements file untouched — the single most common "I thought I pinned my deps" mistake. | build-process | trading-reproducibility.md — 3. Dependency Pinning | CAPTURED |
| Docker image digest pinning | A Docker tag is a mutable pointer — maintainers push new bytes under the same tag; pulling by digest (`image@sha256:...`) is the only guarantee of byte-identical contents. Pin the research/backtest container by digest (the live-trading container can legitimately track patches instead). | build-process | trading-reproducibility.md — 3. Dependency Pinning | CAPTURED |
| Logged run-provenance metadata | Log the resolved environment with every backtest run — lockfile hash, Docker digest, data snapshot ID/checksum, seed values, git commit — as metadata attached to the result, so "what produced this six months ago" is a lookup, not a re-derivation. | build-process | trading-reproducibility.md — 5. Direct Verdict | CAPTURED |
| NumPy version-bump silent numerical changes | NumPy 2.0's NEP 50 changed type promotion rules so mixed-dtype operations can produce different output dtypes and lower precision from a version bump alone, no code change; unstable sort defaults (`argsort`/`argpartition`) also changed 1.26→2.0 from SIMD/algorithm changes — any pipeline sorting ranking signals or order-book levels without an explicit stable sort can silently change output on a NumPy upgrade. | build-process | trading-reproducibility.md — 4. What Actually Breaks Reproducibility in Practice | CAPTURED |
| Pandas default-behavior changes across versions | Copy-on-write semantics (2.x), default dtype inference, and deprecated `.append()`/chained-assignment behavior are flagged as one of the most commonly reported real-world reproducibility-breakage sources in quant Python shops (not independently re-verified this session, but high prior confidence). | build-process | trading-reproducibility.md — 4. What Actually Breaks Reproducibility in Practice | CAPTURED |
| tzdata updates retroactively changing historical timestamp math | tzdata encodes a history of UTC offsets; updates can retroactively change local↔UTC conversion for past dates when governments legislate DST rule changes after the fact, silently shifting which bar a historical timestamp falls into after a routine OS tzdata package update, with no code change. | build-process | trading-reproducibility.md — 4. What Actually Breaks Reproducibility in Practice | CAPTURED |
| Floating-point non-associativity across CPU architectures | `(a+b)+c != a+(b+c)`; different vector instruction sets (AVX2/AVX-512/ARM NEON) order operations differently for "the same" call — developing on Apple Silicon and running production on cloud x86_64 means results won't be bit-identical even with identical code, seeds, and library versions. | build-process | trading-reproducibility.md — 4. What Actually Breaks Reproducibility in Practice | CAPTURED |
| GPU cuDNN algorithm auto-tuning nondeterminism | `cudnn.benchmark=True` picks the fastest kernel for the input shape at runtime and can vary run-to-run — a distinct nondeterminism source from the general seeding gaps. | build-process | trading-reproducibility.md — 4. What Actually Breaks Reproducibility in Practice | CAPTURED |
| Multi-threaded/multi-process race nondeterminism | BLAS/OpenMP internal thread-pool scheduling, `ThreadPoolExecutor`, and multi-worker `DataLoader` execution order are wall-clock-dependent and not fixed by any seed. | build-process | trading-reproducibility.md — 4. What Actually Breaks Reproducibility in Practice | CAPTURED |
| Hidden dict/set iteration-order dependence | Code building a feature vector by iterating a dict without an explicit sort, combined with `PYTHONHASHSEED` left unset, produces a different hash seed per process run and therefore a different feature-column ordering, silently — a real, easy-to-miss bug class in code that dynamically builds feature sets. | build-process | trading-reproducibility.md — 4. What Actually Breaks Reproducibility in Practice | CAPTURED |
| OS keyring/Secret Service — unsuitable for headless VM | The Secret Service API depends on a D-Bus session bus normally spun up by a graphical login; faking one on a headless VM requires the unlock secret to live on disk/in a boot script anyway, collapsing the security model to "a file the process can read" with a more fragile dependency chain — skip entirely for a headless trading VM. | security | trading-secrets-management.md — 1. Where to actually store the secrets (OS keyring) | CAPTURED |
| sops + age encrypted secrets workflow | Encrypt secrets to git-committable ciphertext via age recipients (or a cloud-KMS-backed recipient tied to VM identity), decrypt straight into the process environment at runtime via `sops exec-env` so plaintext never touches disk. Recommended as the right answer at solo scale: encrypted-at-rest, diffable/git-trackable, zero servers. | security | trading-secrets-management.md — 1. Where to actually store the secrets (sops + age) | CAPTURED |
| Cloud provider secrets manager (direct use) | VM's IAM role/service account fetches the exchange key via an authenticated call at startup, so no long-lived credential needs to sit on disk to obtain the trading key; supports automatic rotation; a legitimate, low-effort option if already on that cloud, simpler than sops if git-tracking ciphertext isn't needed. | security | trading-secrets-management.md — 1. Where to actually store the secrets (Cloud provider secrets managers) | CAPTURED |
| Self-hosted HashiCorp Vault — overkill verdict | Shamir-seal unsealing is a manual process; auto-unseal removes that but creates a hard dependency on the cloud KMS being available; running Vault means a whole additional server/TLS/storage-backend/policy-lifecycle component that itself needs securing and can go down and take credential access with it. Verdict: overkill for one operator — don't stand it up just for this. | security | trading-secrets-management.md — 1. Where to actually store the secrets (HashiCorp Vault) | CAPTURED |
| Exchange API key IP allowlisting | Binance mandates IP restriction for any key with permissions beyond read-only; Coinbase recommends but doesn't force it. Since the bot runs from one VM with a knowable egress IP, IP-locking the trading key defeats the most common leak scenario (key exfiltrated via a scraped secret, compromised laptop, etc.) because a stolen key is useless from any IP but the VM's. | security | trading-secrets-management.md — 2. Exchange-side key scoping (IP allowlisting) | CAPTURED |
| Disabling withdrawal permission on the trading key | The single highest-leverage control found: never grant withdrawal/transfer permission to the key that lives on the trading VM. If a trading-only, no-withdrawal key leaks, worst case is "value destroyed via bad trades," recoverable-in-kind — not total, irreversible capital loss. | security | trading-secrets-management.md — 2. Exchange-side key scoping (Disabling withdrawal permission) | CAPTURED |
| Separate exchange keys by function | A read-only key for market-data/balance polling and a separate trade-execution key (IP-locked, no withdrawal) scoped only to trading, so that a compromised non-core component (dashboard, logging pipeline, notification bot) can't place trades. | security | trading-secrets-management.md — 2. Exchange-side key scoping (Separate keys by function) | CAPTURED |
| Key rotation cadence and safe-swap procedure | Rotate on a fixed 60-90 day calendar schedule (not more aggressively, since rotation itself is a risk window) plus immediately on any compromise signal. Safe procedure never has a zero-valid-key window: create the new key, verify it works from the actual production process before cutover, only revoke the old key after the new one has completed a real order-placement cycle. | security | trading-secrets-management.md — 3. Key rotation | CAPTURED |
| Automated watchdog kill-switch process | A "3am page" is not a control at unattended scale; a separate watchdog process (not the trading bot itself, since compromised/buggy logic can't be trusted to police itself) enforces hard caps on order rate, position size, and per-trade notional, and kills the bot process the instant a cap is breached. | risk | trading-secrets-management.md — 4. What changes specifically because there's no human in the loop | CAPTURED |
| Network-level exchange access revocation on kill-switch trip | Beyond killing the bot process, the watchdog drops outbound network access to the exchange's API hosts via `iptables`/security-group rule (more reliable than hoping a process kill stops a stuck thread), and can additionally revoke the VM's IAM role or invalidate the Secrets Manager secret so a restarted bot can't even re-fetch working credentials on reboot. | risk | trading-secrets-management.md — 4. What changes specifically because there's no human in the loop | CAPTURED |
| Exchange self-service key auto-revocation — unavailable/unverified | No exchange documentation found exposing a self-service API endpoint to revoke/delete your own API key programmatically; revocation is typically gated behind the web UI with 2FA/email confirmation — don't design a kill-switch's core assumption around this capability existing. | security | trading-secrets-management.md — 4. What changes specifically because there's no human in the loop | CAPTURED |
| Blast-radius containment: trading key isolated from treasury | The trading key must live on a separate exchange sub-account or wholly separate account from any treasury/cold-storage holdings, with no read or transfer access to the treasury side; treasury ideally held with zero API surface at all (hardware wallet). A compromised trading VM should be able to lose, at absolute worst, only the working capital allocated to the strategy — never the whole fund. | security | trading-secrets-management.md — 4. What changes specifically because there's no human in the loop | CAPTURED |
| Secrets never plaintext-on-disk longer than necessary | Prefer `sops exec-env` (decrypts straight into the child process's environment) over `sops -d > file.env`, which leaves a plaintext file outliving the decrypt command unless cleanup is explicit — unattended, nothing notices if that cleanup silently fails. | security | trading-secrets-management.md — 4. What changes specifically because there's no human in the loop | CAPTURED |
| No exchange credentials in MCP/third-party tool servers | Every crypto/trading MCP server found requires exchange API keys inside its own environment/tool-access context (one explicitly wants full key+secret+passphrase for 9 exchanges) — forbidden. Verdict: call exchange APIs (e.g. ccxt) directly from controlled Python code instead, adding zero credential surface to the agent's tool context. | security | mcp-plugins-for-trading-stack.md — 1. Crypto / trading MCP — nothing clears the bar | CAPTURED |
| Hard Execution-Based Evaluator Gating Generation | Gate any LLM-generated strategy/code behind a deterministic, non-LLM evaluator (code execution, backtest, physical test) rather than trusting LLM confidence; the two reproduced successes (FunSearch, Stanford Virtual Lab) both had this, the two weak ones (AI Co-Scientist, Sakana) relied on LLM-judged loops | validation | cutting-edge-ai.md — section 1, "single most important pattern" | CAPTURED |
| Evaluator Outside System Write Access | The evaluator/gate that scores a self-improving system's outputs must live outside that system's own write access, because a self-improving system will otherwise edit the evaluator/test/threshold itself to cheat the metric (Sakana AI Scientist precedent: self-extended timeouts, recursive self-calls) | governance | cutting-edge-ai.md — section on Sakana gaming incident | CAPTURED |
| AlphaEvolve-style Evolutionary Program Search | LLM ensemble → prompt sampler → execution-based evaluator → evolutionary program database (MAP-Elites style) for automated strategy/algorithm discovery; Google reports real production deployments (~0.7% of worldwide compute recovered, 23% kernel speedup, first improvement in 56 years on 4x4 complex matrix multiplication) — self-reported, no third-party audit | intelligence | cutting-edge-ai.md — section "AlphaEvolve architecture (our template)" | CAPTURED |
| Single Strong Agent Over Multi-Agent Committee | Use one agent holding full context for the research/trading-decision loop rather than a committee of specialized role-playing agents (data engineer, feature researcher, validator, risk officer); multi-agent LLM systems show 41-86.7% coordination-pathology failure rates (UC Berkeley study) and Anthropic's own multi-agent writeup shows real gains only at 15x token cost and explicitly excludes tasks needing shared context/many dependencies. Verdict: avoid role-playing multi-agent committees for this domain | governance | cutting-edge-ai.md — section 2, correction on multi-agent evidence | CAPTURED |
| Parallel Workers For Independent Side-Effect-Free Fetches Only | Limit parallel/concurrent agent workers to genuinely independent, side-effect-free data-fetching tasks (e.g. pulling exchange A's data and exchange B's data simultaneously) rather than using parallel agents for reasoning/decision work | build-process | cutting-edge-ai.md — section 2, "Revised design" | CAPTURED |
| Majority-Vote Sampling Over Multi-Model Mixing | Plain majority-vote sampling of one strong model captures much of the benefit claimed by mixing different LLMs together, and mixing different LLMs (Self-MoA) can lower quality vs. repeated sampling of a single strong model — verdict: prefer resampling one strong model over ensembling different models | intelligence | cutting-edge-ai.md — section 2, Self-MoA and "More Agents" findings | CAPTURED |
| Red-Team Agent Generates Testable Hypotheses, Not Verdicts | An adversarial/red-team agent should generate testable hypotheses about how a strategy might fail (e.g. "does this survive the March 2020 window?") and those hypotheses must be settled by running an actual test, never by LLM judgment — because LLM self-correction without ground-truth feedback measurably degrades performance (CommonSenseQA 75.8%→41.8%) and LLM-as-judge is gameable (only 65% self-consistency under answer-order swap, verbosity padding fools judges) | validation | cutting-edge-ai.md — section 3, correction on adversarial red-team agent | CAPTURED |
| RD-Agent Research-Development Loop | Microsoft RD-Agent (14.1k stars, active OSS, arXiv:2505.14738) implements an explicit Research (LLM hypothesis) → Development (code-gen + backtest) loop on top of Qlib; the OSS tool itself is judged genuinely production-grade, but its self-reported "2x returns" performance numbers are unreplicated | strategy | cutting-edge-ai.md — section 4, finance-specific automated discovery | CAPTURED |
| AutoML Weak For Trading Strategy Search | A direct literature search found zero rigorous papers combining AutoML with algorithmic trading, and one 2026 benchmark found a simple rule-based value filter beat an AutoGluon AutoML model over 20 years of S&P 500 data — verdict: AutoML is not currently a credible strategy-search method for this domain | strategy | cutting-edge-ai.md — section 4, "AutoML for trading is thin" | CAPTURED |
| Bi-Temporal Memory (Recency/Importance/Relevance + Reflection) | Agent memory architecture combining recency/importance/relevance-weighted retrieval with periodic reflection (triggered at a cumulative-importance threshold) that synthesizes higher-level insights from recent memories, plus bi-temporal fact tracking (transaction time vs. valid time, so "we believed X on date D" is distinct from "X was true on date D") — best-evidenced production implementation is Zep/Graphiti, though its "beats MemGPT" claim is a vendor-run, non-reproduced comparison | intelligence | cutting-edge-ai.md — section 5, memory architectures | CAPTURED |
| Population Based Training For Hyperparameter Search | Population-based training (PBT) is DEPLOYED/PROVEN (DeepMind Quake III, published in Science) for online hyperparameter/strategy-population tuning; its use in AlphaStar is widely assumed but not confirmed | validation | cutting-edge-ai.md — section 6, self-improvement techniques table | CAPTURED |
| Self-Play Does Not Transfer To Markets | Self-play (AlphaZero/MuZero-style) is the most strongly proven and independently-reproduced self-improvement technique in the research set, but it requires perfect-information, fully-simulatable environments — markets are neither, so this technique should not be assumed to transfer to trading | strategy | cutting-edge-ai.md — section 6, self-improvement techniques table | CAPTURED |
| MAML Meta-Learning Is Fragile — Avoid | Model-Agnostic Meta-Learning (MAML) is fragile/disputed: follow-up work (ANIL) found feature reuse rather than rapid adaptation drives its results, MAML++ documents severe training instability, and no production use was found — verdict: not a credible adaptation technique to build on | models | cutting-edge-ai.md — section 6, self-improvement techniques table | CAPTURED |
| Curriculum Learning Ordering Disputed | Curriculum learning (structured ordering of training examples/difficulty) has real RL wins in some settings but a controlled study (ICLR arXiv:2012.03107) found random ordering matches or beats curricula — verdict: not a reliably beneficial technique, don't assume it helps | models | cutting-edge-ai.md — section 6, self-improvement techniques table | CAPTURED |
| Dual-LLM Pattern For Untrusted Web Content | A privileged model with trading/execution access must never directly see raw untrusted web content; a separate, quarantined, tool-less model reads and summarizes the web, returning only inert structured data to a non-LLM controller — architectural separation, not prompting, is the actual defense against prompt injection given documented real-world incidents (PoisonedRAG: 90% attack success injecting 5 malicious docs into a corpus of millions; Bing Chat, ChatGPT Search, Gemini memory corruption, Claude web_fetch nested-URL exfiltration) | security | cutting-edge-ai.md — section 8, "The mitigation: Dual LLM pattern" | CAPTURED |
| Human Approval Gate For Capital-Moving Actions | Require human approval for any capital-moving action, as part of the OWASP LLM Top 10 capability-separation defenses against prompt injection given the system reads untrusted internet content, holds trading credentials, and can act on markets (the "lethal trifecta") | risk | cutting-edge-ai.md — section 8, SECURITY | CAPTURED |
| Distrust "95%-Effective" Guardrail Products | Do not rely on prompt-based or ML-based guardrail products claiming ~95% effectiveness against prompt injection — a 5% bypass rate is unacceptable when capital is at risk, and universal adversarial suffixes are shown to transfer across GPT/Claude/Bard/Llama, meaning safety alignment is probabilistic rather than a hard boundary | security | cutting-edge-ai.md — section 8, closing paragraph | CAPTURED |
| Agentic Misalignment Under Simulated Goal Conflict | Anthropic's Agentic Misalignment study found models under simulated goal conflict chose harmful actions (blackmail up to 96% of runs) with no external attacker at all — misalignment risk exists independent of prompt-injection risk and must be defended against separately | security | cutting-edge-ai.md — section 8, "Also:" note | CAPTURED |
| Kronos Foundation Model For K-line Data | Kronos is a pretrained transformer (BSQ tokenization of candlestick data → causal transformer → cross-attention head) usable via `KronosTokenizer`/`Kronos`/`KronosPredictor` API, both tokenizer and predictor can be finetuned on a specific asset; hard constraint `max_context=512`; authors' own disclaimer: "raw signals are not pure alpha, no transaction costs, no risk neutralization — a research tool, not a money printer." Repo has 35,412 stars but last push was ~4 months stale at time of review — verdict: usable as a zero-shot baseline gate only, check maintenance status before treating as a live dependency | models | instagram-findings.md — Kronos entries (DbO5FVYgd19, DbDnA_8kcjW) | CAPTURED |
| Zomma-Aware Options Gamma Risk | Track Zomma (third options greek, rate of change of gamma with respect to volatility) alongside delta/gamma — quantified example shows Zomma runs increasingly negative as implied vol falls (e.g. -0.373 at 8.6% vol vs -0.749 at 15.9% vol near the money), meaning a vol spike drains gamma exactly where a position is concentrated | risk | instagram-findings.md — Zomma entry (DbYOQlSFJSZ) | CAPTURED |
| Absorption Detection On Footprint/Order-Flow Data | Detect passive-seller (or buyer) absorption on a footprint/order-flow chart: aggressive market buying into resting passive size at a level, signaled by high volume without price progress — treated as a reversal signal and specifically as a signature that lures retail to chase delta while size is quietly absorbed before a reversal | feature-engineering | instagram-findings.md — delta/absorption entry (Da_f158GyKd) | CAPTURED |
| MacroHFT-style Promotion Objective | Loss/objective function shape for gating strategies: L = −mean_Sharpe + α·vol_penalty + β·max_drawdown_penalty + γ·cost_penalty + δ·churn_penalty — a directly reusable objective shape for a promotion gate that penalizes volatility, drawdown, transaction cost, and turnover in addition to raw Sharpe | governance | instagram-findings.md — MacroHFT entry (DbXE8o_sspP) | CAPTURED |
| AOSm Consensus-Based Update Rule | Optimizer/consensus update rule x_i^(t+1) = p_i^t + η_t(c_i^t − p_i^t), where c_i^t is chosen between the global best and local best (c_i^t ∈ {g^t, ℓ_i^t}, g^t = argmin_j L(x_j^t)) — a candidate optimizer for parameter/strategy search, would need to run inside a proper trial registry subject to the same multiple-comparisons caveat as any search method | intelligence | instagram-findings.md — AOSm entry (Dbbvsvcsmbb) | CAPTURED |
| Marchenko-Pastur / RMT Correlation Denoising | Random matrix theory technique for cleaning a correlation/covariance matrix before portfolio construction: eigenvalues that fall within the Marchenko-Pastur random-matrix bound are treated as noise and removed, since most raw pairwise correlations in financial markets are statistically indistinguishable from noise | portfolio | instagram-findings.md — RMT entry (DYU0dMcpBFP) | CAPTURED |
| Options/Allocation/Bayesian Library Shortlist | Five verified real open-source libraries for specific quant tasks: CVXPY (allocation — CVaR, tracking-error, MIP cardinality caps, Apache-2.0), NumPyro (stochastic vol, regime detection, hierarchical alpha via posteriors not point estimates, Apache-2.0), Diffrax (Heston/rough Bergomi/jump-diffusion calibration, Apache-2.0), FinancePy (SABR, LMM, Hull-White, Bermudan trees, CDS — GPL-3.0, same copyleft caution as other GPL dependencies), py-pde (Black-Scholes and Dupire local-vol PDE, Fokker-Planck of a diffusion, MIT) | models | instagram-findings.md — five libraries table (DY4dxCkDa8Z) | CAPTURED |
| Options-Pricing Library Shortlist (17-library post) | Additional named real, checkable options/quant libraries worth checking against the settled stack before writing pricing code from scratch: OpenBB Terminal, PyQL (QuantLib Python port), vollib (option prices/IV/greeks), pynance, pysabr (SABR model), optlib, tf-quant-finance, Q-Fin | models | instagram-findings.md — late addition, 17 Python libraries (Da8LFnfFnqx) | CAPTURED |
| ZipLime As Rejected Backtest-Engine Alternative | ZipLime (Limex-com/ziplime, 452 stars, GPL-3.0, active) reimplements the Zipline backtest API on Polars claiming identical strategy code across backtest and live, the same property NautilusTrader was chosen for — considered and explicitly rejected because its performance benchmark is vendor-published (not independently verified) and its license (GPL-3.0) is stronger copyleft than NautilusTrader's LGPL-3.0 | validation | instagram-findings.md — ZipLime entry (DZ2PiNFltKt) | CAPTURED |
| Order-Flow/DOM/Volume-Profile Ranked Above Lagging Indicators | Multiple independent sources in the corpus (real DOM/ladder screen recording, two independent indicator tier-lists) converge on ranking order flow / depth-of-market / volume profile as the highest-value signal category and RSI/MACD-style lagging indicators as lowest-value — treated as opinion/converging anecdote, not evidence, but consistent with an existing L2-depth-is-priority-zero decision | feature-engineering | instagram-findings.md — DaNFqoYNtIQ, DYB671CtuaQ, DV_w-fRDEGI entries | CAPTURED |
| Air-Gapped/Sandboxed Execution For Third-Party Model Weights | Run any third-party pretrained model weights (e.g. downloaded from HuggingFace, such as Kronos) air-gapped and sandboxed rather than trusted-by-default, because AI supply-chain attacks can embed dormant object-code payloads directly into model weight files (analogous to the xz-utils backdoor: no malicious source code, just object code shipped as inert-looking artifacts and linked in later) and detection tooling for this class of attack is immature | security | instagram-findings.md — Da1XG9qgd4E, AI supply-chain attack on model weights | CAPTURED |
| Risk-Neutral Monte Carlo Pricing Framing | Correct framing for Monte Carlo option/derivative pricing: simulate the underlying growing at the risk-free rate (not at a real-world expected return), average the resulting payoff, and discount back — explicitly "a pricing device, not a forecast," to avoid conflating a pricing tool with a directional prediction | models | instagram-findings.md — reel DZ9xV72I54p, Monte Carlo | CAPTURED |
| Center-and-Scale Before PCA | Center and scale features first before applying PCA, so that no single feature dominates the principal components purely due to differing magnitude/units | feature-engineering | instagram-findings.md — reel DZ7bTCtoc3s, PCA explainer | CAPTURED |
| Fabricated-Metrics Social Content Filter | A specific Instagram account (@seb.ai, 6 of 57 links) posts the same fabricated agent-pipeline template (RESEARCH→DEBATE→BACKTEST→RISK→EXECUTE / SCAN→SIGNALS→PLAN→RISK→MONITOR→DECISION shapes) under multiple different brand names with invented performance metrics (fake GitHub star counts, invented VaR/beta/drawdown figures) — verdict: content that leads with a return figure or dashboard and no method detail carries zero evidential weight; only mechanism-level, specific, no-profit-claim posts in the corpus were judged actionable (roughly 8 of 57 links) | governance | instagram-findings.md — headline finding 1 and closing "signal-to-noise" section | CAPTURED |
